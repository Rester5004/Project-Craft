# TODO — 테크트리 실현 가능성 검토

> 조사일 **2026-08-05** · 정본 3종(Notion `기획 백로그(수정본)` · Obsidian 캔버스 6장 · `레시피.md`)과
> 현재 프로젝트(레시피 170 · 아이템 223 · 블록 56)를 전수 대조한 결과.
>
> **결론: 지금 상태로는 0티어에서 막혀 아무 데도 갈 수 없다.** 막는 것은 아래 §B 의 P0 네 가지다.

**이번에 확정한 것**

| 항목 | 결정 |
|---|---|
| 추출 산출 배정 | **캔버스를 정본으로 한다** (현재 구현된 레시피 9개 + `ExtractionTable` 이 그대로 정본) |
| 2티어 수치 | **`레시피.md` 값을 그대로** 쓴다. 거기에도 없는 칸만 `(미정)` |

> ## 2026-08-06 반영 — 결정 8건을 에셋·코드에 넣었다
>
> **✅ 해결됨** — A-1(유황석·미가공 우라늄·반짝이는 돌) · A-4(0티어 부트스트랩) ·
> B-1(도구 레시피 두 벌) · B-3(`build_extractor` 빈 산출) · B-5 일부(동일 중복 3쌍) ·
> C-4(감별기 — 삭제로 종결) · C-8 일부.
> **A-2 · A-3 · A-5 · A-6 은 그대로 열려 있다**(A-3·A-5·A-6 은 "추후"로 미루기로 결정).
> 자세한 내용은 각 절의 취소선과 §E 를 볼 것.

---

## 0. 용어 못박기

세 문서가 같은 말을 다르게 써서 생긴 오해가 많았다. 아래를 기준으로 한다.

- **`n-m티어 추출기`** — **`n` = 들어가는 자원의 계열**(0=돌 · 1=마력석 · 2=운석),
  **`m` = 추출기 등급**. 같은 계열에서 m 이 오를수록 산출 종류가 늘어난다(슈퍼셋).
- **`m` 은 해금 시점과 별개다.** `n=2 m=0` 추출기가 테크상 3티어에 가서야 열려도 이상하지 않다.
  → 등급표(`ExtractionTable`)와 **해금표(건설 레시피)는 따로 정해야 한다.**
- **구조는 `분쇄기 → 추출기`.** 분쇄기는 주 산출물만 100% 로 내고, **부산물은 추출기가 확률로** 낸다.
  Notion 의 "분쇄/추출 테이블"이 둘을 한 장에 합쳐 놓아 분쇄기가 부산물을 내는 것처럼 읽힌다.
- ⚠ **Notion §2 의 "자원 생성기 = 지형 기반 자동 추출"은 추출기가 아니다.**
  프로젝트의 `자원 생성기`·`펌프`·`지열 발전기` 쪽이다. 같은 문단에 있어 계속 혼동을 부른다.

---

## A. 먼저 정해야 할 것

레시피를 만들기 전에 답이 필요한 것들. **정하지 않고 만들면 또 갈아엎게 된다.**

### ~~A-1. 유황석 · 반짝이는 돌 · 미가공 우라늄을 어디서 낼 것인가~~ ✅ 2026-08-06 해결

**0계열 추출을 가공 전 단계로 바꿔서 풀었다** — 1계열에 얹는 권장안 대신, 0계열이 이미 내던
`황 가루`·`우라늄 가루`(완제품이라 그 뒤 기계를 죽이던 것)를 원료로 되돌렸다.

| 무엇을 | 어떻게 |
|---|---|
| 유황석 | `extract_stone_powder` 의 `황 가루 5%` → **`유황석 5%`**(0-2 개방). 화학 처리기 3레시피가 살아났다 |
| 탁한 우라늄 | 같은 곳의 `우라늄 가루 1%` → **`탁한 우라늄 1%`**(0-3 개방). `미가공 우라늄` 을 개명해 재사용 |
| 반짝이는 돌 | **삭제.** 감별기까지 함께 지웠다 — 루비·사파이어·다이아몬드는 1계열 추출이 이미 낸다 |

새 우라늄 사슬 — `탁한 우라늄` →화학(+산성 용액 ×2)→ `우라늄 조각` →전기로 `smelt_uranium`→ `우라늄 주괴`
→압축기 `uranium_concentrate`(×10)→ `우라늄 농축물`. 뒤의 두 레시피는 이번에 새로 만들었다.

### A-2. 추출기 12종의 해금 시점 🔴

`m` 은 등급일 뿐이라 **언제 만들 수 있는지가 어디에도 없다.**
현재 추출기 13종 중 **건설 레시피가 있는 것은 `0-1티어` 하나뿐**이다.

- **(권장) 계열 해금과 등급 업그레이드를 분리** — `n` 계열은 그 메인자원에 닿을 때 열리고(0=시작, 1=지하층,
  2=외곽), `m` 은 기존 추출기 + 부품으로 업그레이드(레시피.md 의 `0-2티어 = 컴퓨터 칩2 + 0-1티어 + 니켈 주괴1`
  패턴). 표 하나로 12칸을 채울 수 있다.
- (대안) 12종 각각 독립 레시피 — 자유롭지만 표가 12줄로 늘고 서로 어긋나기 쉽다.

### A-3. 마력 파편의 최초 획득처 🔴 — **추가 예정 (2026-08-06 사용자 결정: 이번엔 손대지 않음)**

**어느 문서에도 없다.** 있는 것은 증식뿐이다 — `마력 파편1 + 구리 주괴10 → 마력 파편2`.
0에서 시작할 수 없으므로 **0티어 마법 전체와 1티어 진입이 통째로 막힌다**
(초급 재단 · 물 · 용암 · 자원 생성기 · 마법이 부여된 전도체 가루).
**수동 0-0티어 추출기(`돌10 + 크랭크 + 마력파편`)도 여기 걸려 아직 못 만든다.**

- **(권장) 마력석에서 낸다** — `0티어.canvas` 의 "지하층에서 마력광석 획득 → 기초 마법 시작"과 맞는다.
  마력석 분쇄/추출 산출에 마력 파편을 넣거나, 마력석 → 마력 파편 조합대 레시피.
- (대안) 월드 드랍 / 시작 지급 — 지하층 진입 전에 마법을 열고 싶을 때.

### ~~A-4. 0티어 부트스트랩을 어떻게 풀 것인가~~ ✅ 2026-08-06 해결

**수동 분쇄기를 값싸게 만들고 옛 재료를 전기판으로 내려서 풀었다.**

```
수동 분쇄기 = 돌 10 + 크랭크 1        크랭크 = 돌 2 (칼 필요)  ← 순환 없음. 여기서 시작한다
전기 분쇄기 = 크랭크 1 + 철 주괴 2 + 철판 2 + 돌 4   ← 옛 수동 레시피를 그대로 물려받았다
0-0티어 추출기(전기) = 크랭크 1 + 구리 주괴 2 + 구리판 2 + 돌 4  ← 옛 수동 추출기 재료
수동 0-0티어 추출기  = 돌 10 + 크랭크 1 + 마력 파편 1  ← A-3 이 풀려야 만들 수 있다
```

`수동 분쇄기` 는 플레이스홀더 아이템이던 것을 **정식 기계 `Machine:ManualPulverizer` 로 승격**했다
(tier 0 · `recipeGroupId = Pulverizer` · `manualStepRatio 0.05` · 무전력).
`MachineAliases` 의 임시 줄 `{수동 분쇄기 → 전기 분쇄기}` 는 이때 지웠다.
⚠ 벽돌은 이제 어느 0티어 건설 레시피에도 안 쓰인다 — `화로`·`압연기`만 여전히 요구한다.

### A-5. 코어 업그레이드를 무엇으로 구현할 것인가 🟡 — **코어 조합기 UI 에 붙일 예정 (2026-08-06 결정)**

캔버스에는 1차·2차가 있는데 **Notion 에도 프로젝트에도 없다.** `CraftingTableBlock.tier` 로 레시피를 거르지만
**티어를 올리는 수단이 없다.**

- 캔버스: 1차 = `마법이 부여된 전도체 가루` ×1 · 2차 = `마력 칩`
- **(권장) 코어 블록 교체 레시피** — 기존 기계 업그레이드와 같은 방식(`중급 재단 = 초급 재단 + 금 주괴10`).
  `tier` 가 다른 `CraftingTableBlock` 을 하나 더 만들고 그것으로 바꾼다. 코드 변경 0.
- (대안) 전용 상호작용 — 코어에 아이템을 넣으면 티어가 오른다. 새 코드가 필요하다.

### A-6. 전압 2풀을 도입할 것인가 🟡 — **추후 구현 (2026-08-06 결정: 미룬다)**

Notion 은 저전압/고전압 2풀이고 "전기 분해기는 고전압 필수"인데, **현재는 단일 풀**이라 표현되지 않는다.

- (권장) **미룬다** — 지금 막힌 것들(A-1~A-4)과 무관하고, `MachineInstance`·`PowerLinkMode`·세이브까지
  건드려야 한다. 0·1티어가 도는 것을 먼저 확인하고 손대는 게 낫다.
- (대안) 지금 넣는다 — 2티어 설계가 전압 전제라면 나중에 더 비싸진다.

---

## B. 결정과 무관하게 지금 고쳐야 할 결함

### 🔴 P0 — 이것 때문에 게임이 안 돈다

**~~B-1. 망치·드라이버·곡괭이를 만들 수 없다~~** ✅ 2026-08-06 정정 + 해결

⚠ **이 항목의 진단이 틀렸었다.** `Craft_Hammer`·`Craft_Driver`·`Craft_Pickaxe` 는 일반 `Recipe` 가 아니라
**`ToolRecipe`** 다 — 재료가 구체적 아이템이 아니라 **부품 종류**라서 `inputs` 가 비어 있는 것이 **정상**이고,
조합대가 부품 칸을 따로 띄운다. 즉 이쪽이 정본이고 정상 동작한다.

실제 고장은 반대쪽이었다: 재질을 고정한 옛 조립 레시피 7개(`hammer` `stone_hammer` `iron_hammer`
`craft_stone_hammer` `craft_iron_hammer` `driver` `screwdriver`)를 `ToolAssetGenerator` 가 산출 참조만
끊어 놓고 남겨 둔 것. **전부 지웠고** ToolRecipe 3개를 `hammer`·`driver`·`pickaxe` 로 개명했다.

부품 레시피는 원래부터 정상이었다 — `stick`(돌1 → 나무 자루) · `hammer_head`(돌2 → 나무 망치 머리) ·
`stone_hammerhead` · `iron_hammerhead`. **망치·드라이버는 이제 만들 수 있다.**
⚠ **곡괭이는 여전히 못 만든다** — `*_pickaxe_head` 16재질 전부 레시피가 없다(§C-9).

**B-2. 판재를 철판·실리콘 판밖에 못 만든다** → 컴퓨터 칩(구리판 ×10)이 막히고,
그 뒤의 **레이저 가공기 · 자동 조합기 · 마력 칩 · 0-2티어 추출기가 전부 죽는다.**
(§C-1 에서 만든다)

**~~B-3. `build_extractor` 의 산출이 삭제된 에셋~~** ✅ 2026-08-06 — `Machine:Extractor00Plus` 로 채웠고
에셋 이름도 `extractor00plus` 로 바꿨다.

**B-4. 2티어 건설 레시피 11개가 입력 0** — `RecipeSolver.HasInputs` 가 막아 영원히 못 만든다.
이름은 `advanced_crafter` `blast_furnace` `distiller` `extractor03` `gas_generator` `hydroponics`
`item_enhancer` `precision_fabricator` `pump` `upgrade_module_speed` `upgrade_module_efficiency` 로
정리됐지만 **재료는 여전히 비어 있다** — §C-7 의 아이템 4종이 없어 채울 수 없다. (§C-6)

### 🟡 P1 — 조용히 어긋나 있는 것

**B-5. 중복 레시피** — `SelectRecipe` 가 첫 것만 쓰므로 뒤쪽은 죽은 레시피다.
2026-08-06 에 7쌍을 정리했다(취소선). 남은 것:

```
[초급 재단]    magic_lava / magic_water          ← 입출력이 같아 물과 용암을 구분할 수 없다 ⚠
[핵발전소]     nuclear_power / power_nuclear
[CoreCrafter vs 제단] apple_tree_seed · enchanted_conductor_powder · enchanter ·
                      resource_generator_t0 · resource_generator_t0_enhanced   ← 의도된 이중 경로인지 확인 필요
~~[코어 조합기] driver / screwdriver · craft_iron_hammer / iron_hammer · craft_stone_hammer / stone_hammer~~ ✅ 삭제
~~[감별기] appraise_gems / appraise_gems_2~~ ✅ 감별기째 삭제
~~[전기 분해기] electrolyzer_water ×2 · [마나 용해기] mana_from_crystal ×2 · mana_from_shard ×2~~ ✅ 하나씩만 남김
```

`magic_lava`/`magic_water` 는 단순 중복이 아니라 **설계 결함**이다 — 재료가 같아
(양동이 + 마력 파편2) 플레이어가 물과 용암을 고를 수 없다. 재료를 다르게 하거나 기계를 나눠야 한다.

**B-6. 발전기 레시피 10개가 전부 무의미하다** — 발전은 `MachineInstance.TickGenerator` 가 연료 슬롯에서
직접 태우므로 레시피를 보지 않는다. 화력 발전기 7개 · 핵발전소 2개 · 지열 1개 전부 죽은 에셋이다.
`석탄 or 갈탄` · `석탄 또는 갈탄` · `석탄/갈탄/석유 중 1` · `석탄 / 갈탄 / 석유` 같은
**"이 중 아무거나" 가짜 아이템 4종**도 여기서 나온 것이라 같이 정리 대상이다.

**B-7. 이름이 내용과 다르다**
- `craft_stick` : **돌 1개 → 나무 막대** / `hammer_head` : **돌 2개 → 나무 망치 머리** — 돌로 나무를 만든다
- `blast_steel`(강철)이 **화로**에 붙어 있다 — Notion·`레시피.md` 는 **용광로** 담당
- 1계열 사슬 레시피 이름이 옛 아이템 이름 그대로다 — `crush_ore` `crush_crushed_ore` `crush_ore_grains` ·
  `extract_crushed_ore` `extract_ore_grain` `extract_shiny_powder`
  (아이템은 `마력석 조각/가루/미세한 마력석 가루` 로 바뀌었다)

**B-8. 수량이 문서마다 다르다** — 티타늄 주괴가 Notion ×4 / `레시피.md` ×3. **현재 에셋은 ×4.**
`레시피.md` 우선으로 정했으므로 **에셋과 Notion 을 ×3 으로 맞춘다.**

### 🟢 P2 — 미구현이라 비어 있는 것 (버그 아님, 기능이 없을 뿐)

마나 용해기(유체 미구현) · 변압기(전압 체계 미구현) · 전기 분해기의 물 → 수소/산소(기체 미구현)
(~~감별기 확률 분류~~ — 감별기는 삭제됐다)

---

## C. 만들어야 할 레시피

### C-1. 판재 — **P0** (없으면 컴퓨터 칩 이후가 전부 막힌다)

압연기/압축기에 `철판`·`실리콘 판`만 있다. 나머지를 만든다.

- [ ] 구리판 ← 구리 주괴 ×1 (압연기 / 압축기)
- [ ] 금 판 ← 금 주괴 ×1
- [ ] 은 판 ← 은 주괴 ×1
- [ ] 청동 판 ← 청동 ×1
- [ ] 인바 판 ← 인바 ×1
- [ ] 금속 판 ← 금속 주괴 ×1 — `compressor_metal_plate` 가 이미 있으나 **`금속 주괴` 자체를 못 만든다**.
      `금속`/`금속 주괴` 가 "아무 금속이나"를 뜻하는 가짜 아이템인지 실제 아이템인지 먼저 정할 것

### C-2. 0티어 부트스트랩 — 대부분 해결

- [x] 첫 기계 경로 — **수동 분쇄기 = 돌 10 + 크랭크 1** (2026-08-06). 철도 벽돌도 필요 없다
- [x] 분쇄기 건설 레시피에서 순환 제거 — 전기판이 옛 재료(크랭크+철+철판+돌)를 물려받았다
- [ ] 마력 파편 최초 획득 경로 (A-3) — **추가 예정**. 이게 없으면 0티어 마법과 수동 추출기가 막힌다
- [ ] 손 채굴로 첫 금속을 줄지 — 지금은 분쇄기 → 추출기로만 철을 얻는다(경로는 뚫려 있음)

### ~~C-3. 1계열 부산물 3종~~ ✅ 2026-08-06 — 0계열 산출을 가공 전 형태로 바꿔 해결 (§A-1)

### ~~C-4. 감별기 확률 분류~~ ✅ 2026-08-06 — 감별기·반짝이는 돌·레시피 2개를 모두 삭제

### C-5. 건설 레시피 — **P1** (기계는 있는데 만들 방법이 없는 것)

추출기 (해금 시점은 A-2 결정에 따름)
- [x] 0-0티어(수동) `manual_extractor_t00` · 0-0티어(전기) `extractor00plus` — 2026-08-06
      (수동 쪽은 마력 파편이 없어 실제로는 아직 못 만든다 — A-3)
- [ ] 0-3티어 `extractor03` — 에셋은 있으나 입력 0 (§C-6)
- [ ] 1-0 · 1-1 · 1-2 · 1-3티어
- [ ] 2-0 · 2-1 · 2-2 · 2-3티어
- [x] 0-1티어 `extractor01` · 0-2티어 `extractor02` — 원래 있었고 이름만 정리

그 밖 기계 9종 — 전부 `철근 콘크리트 + 부품` 패턴(Notion 1티어 공장 표)
- [ ] 변압기 · 화학 처리기 · 시멘트 공장 · 유리 가공기 · 원유 채굴기 · 정유기 · 지열 발전기 · 핵발전소
- [ ] 전기로(1티어 화로) · 고전압 전기로(2티어 화로) — 화로 업그레이드 경로
      ⚠ **전기로가 없으면 tier 1 제련(납·니켈·주석·우라늄)을 못 한다** — 화로는 tier 0 이라 목록에서 걸러진다

### C-6. 2티어 — **P2** (`레시피.md` 값 그대로)

- [ ] 정밀 세공기 = 레이저 가공기 ×1 + 철근 ×5 + `(미정)`
- [ ] 2티어 합금 재련기 = 1티어 합금 재련기 ×1 + 기계 강화 모듈 ×1
- [ ] 용광로 = 철근 ×50 + 2티어 합금 재련기 ×1 + 기계 강화 모듈 ×1
- [ ] 고급 조합기 = 티타늄 주괴 ×100 + 컴퓨터 칩 ×20 + 마력 결정 ×20 + 정밀 세공기 ×1
- [ ] 가스 발전기 = 철근 ×20 + 강화 합금 ×20
- [ ] 0-3티어 추출기 = 0-2티어 추출기 ×1 + 강화 합금 ×10 + 기계 강화 모듈 ×1
- [ ] 강화 합금 ×5 = 인바 주괴 ×5 + 청동 주괴 ×5 + 철 주괴 ×5 (2티어 합금 재련기)
- [ ] 기계 강화 모듈 = 마력 결정 ×20 + 철근 ×10 (정밀 세공기)
- [ ] 모터 / 베어링 / 프로펠러 = 강화 합금 ×10 (정밀 세공기) — 지금 입력 0
- [ ] 강철 주괴 ×1 = 철 주괴 ×2 (용광로) — 지금 `blast_steel` 이 화로에 있고 입력 0
- [ ] 티타늄 주괴 ×1 = 티타늄 조각 ×3 (용광로) — 지금 화로에 ×4
- [ ] 펌프 · 증류기 · 수경 재배기 · 업그레이드 모듈 2종 · 아이템 강화기 = **`(미정)`**

### C-7. 새로 만들어야 할 아이템

- [ ] 강화 합금 · 기계 강화 모듈 · 2티어 합금 재련기 · 강철 주괴 (§C-6 에서 쓴다)
      → **이 4종이 없어서 §B-4 의 입력 0 레시피 11개를 못 채운다.** 여기가 다음 병목이다
- [ ] 나무 씨앗 (`apple_tree_seed` 의 재료인데 만들 방법이 없다)
- [x] 우라늄 주괴 — `smelt_uranium`(우라늄 조각 ×4 → ×1, 전기로) 추가 (2026-08-06)
- [ ] 오스뮴 주괴 · 토륨 주괴 — 조각은 나오는데 **제련 레시피가 없다**

### C-8. 정리 (만드는 게 아니라 지우는 것)

- [x] 동일 중복 3쌍 · 죽은 도구 조립 레시피 7개 삭제 (2026-08-06, §B-5)
- [ ] 발전기 죽은 레시피 10개 (§B-6) · 가짜 연료 아이템 4종
- [ ] `magic_lava`/`magic_water` 재료 구분 · `nuclear_power`/`power_nuclear` 중복
- [ ] 도구 부품 재질별 미사용 에셋 40여 종 — `ToolAssetGenerator` 가 만들었으나 레시피가 없다.
      도구 체계를 정식화할 때 같이 정할 것 (지금 지우면 되살리기 번거롭다)

### C-9. 곡괭이를 만들 수 없다 — **P1** (B-1 에서 새로 드러난 것)

`Craft_Pickaxe`(→ `pickaxe`) ToolRecipe 는 정상인데 **부품이 없다.**

- [ ] `*_pickaxe_head` — 16재질 전부 레시피가 없다 (망치 머리는 나무·돌·철 3종이 있다)
- [ ] `*_rod` — `wood_rod`(`stick`) 하나뿐. `stone_rod`·`iron_rod` 등이 없다

---

## D. 정본 문서 정리

### D-1. Notion 에 반영한 것 (2026-08-05 완료)

대상: `📔 기획 백로그 (수정본)` — 부분 치환으로만 고쳤고, 수정 전 원문은 스크래치패드에 백업했다.
**절이 삭제되거나 표 구조가 깨진 곳은 없다**(수정 전후 절 제목 목록 대조 완료).

1. 0/1/2계열 분쇄·추출 테이블의 부산물을 **캔버스 값**으로 교체. 열리는 등급 `(m0~m3)` 을 함께 표기
2. 주 산출물 열의 옛 이름 → 새 이름 (마력석 조각/가루/미세한 마력석 가루 · 운석 조각/가루/미세 가루).
   0계열도 `가루` → `돌 가루` 로 명시
3. mermaid 3개를 표와 맞춤 — 예전엔 표보다 한 칸 어긋나 있었고 메인자원 노드가 빠져 있었다
4. "추출기 티어 구조" → **"추출기 구조: `n-m` — 계열 × 등급"** 으로 다시 씀.
   n·m 정의, 슈퍼셋, **해금 시점은 m 과 별개**, 등급별 속도·확률 배수표
5. §2 첫머리에 **"자원 생성기는 추출기와 다른 기계다"** 경고 추가
6. 자원 목록의 티어·획득 방법을 캔버스 배정으로 (금속 11종 · 비금속 7종).
   출처가 사라진 것(유황석·반짝이는 돌·미가공 우라늄)은 `⚠ 출처 미정 (A-1)` 로 표시
7. 2티어 공장 표를 `레시피.md` 값으로 채움. 없는 칸은 `(미정)`.
   `2티어 업그레이드 모듈` → **`기계 강화 모듈`** 로 이름 정정
8. **코어 업그레이드** 표 신설 (1차 = 마법이 부여된 전도체 가루 ×1 · 2차 = 마력 칩 ×1)
9. 티타늄 주괴 ×4 → **×3**, 담당 기계를 화로 → **용광로**로 (제련 표·주괴 표 두 곳)
10. 최근 변경 요약 callout 에 이번 갱신 8줄 추가 + **"검토 결과는 저장소 TODO.md 에 있다"** 안내

곁들여 고친 것 — `마법 가루`가 갈 곳을 잃었다는 표시, `나무 씨앗` 획득 경로 부재 표시,
`반짝이는 가루` → `미세한 마력석 가루` 로 행 갱신

### D-2. 2026-08-06 변경을 정본 문서에 반영 (완료)

**Notion `📔 기획 백로그 (수정본)`** — 부분 치환만 사용, 수정 전후 재-fetch 대조 완료
1. 0계열 표·mermaid 의 `황 가루` → `유황석`, `우라늄 가루` → `탁한 우라늄`.
   mermaid 에 화학 처리기 후속 노드 2개 추가 + "추출 산출은 가공 전 형태로" 경고문
2. 자원 목록 — `미가공 우라늄` 행을 `탁한 우라늄`(0계열 m3 1%)으로, `우라늄 조각` 행 신설.
   `유황석` 은 `⚠ 출처 미정` → `돌 가루 추출 (m2) 5%`
3. 주괴 표 — `우라늄 주괴`(조각 ×4, 전기로) 신설, `우라늄 농축물` 을 압축기(주괴 ×10)로
4. 보석류 표 — `반짝이는 돌` 행 삭제, 루비·사파이어·다이아몬드를 1·2계열 추출 출처로
5. 0티어 공장 표 — 분쇄기/추출기 행을 **수동·전기 4행**으로 분리, `감별기` 행 삭제
6. 제련 표 제목을 `(화로 · 전기로)` 로, ~~"화로는 0티어라 tier 1 제련을 못 돌린다" 경고~~ + 우라늄 주괴 행
   (⚠ 이 경고는 **§F 에서 사용자가 뒤집었다** — 화로는 티어 무관, 티타늄만 용광로)
7. 가공 레시피 표 — 감별기 행 삭제, 화학 처리기 우라늄 행 수정, 압축기 행 추가
8. 인프라 규칙의 "0티어는 전력을 쓰지 않는다" → 수동/전기판이 따로 있다는 설명으로
9. callout 에 2026-08-06 요약 8줄

**Obsidian** — 수정 전 원본은 스크래치패드에 백업
- `자원과 그 가공방식.canvas` : `황 가루(5%)` → `유황석(5%)`. (`탁한 우라늄(1%)` 은 이미 반영돼 있었다)
  + 가공 전 형태 원칙과 후속 사슬을 적은 메모 노드 1개 추가 (99 → 100 노드)
- `레시피.md` : 0티어 절의 수동 분쇄기·수동 추출기 재료 교체(+ 전기판 2줄 신설),
  없어진 돌덩이 3종을 자갈/모래/돌 가루로, 1티어에 화학 처리기·전기로·압축기 우라늄 사슬 추가

### D-3. 아직 어긋난 채 남은 것

- **`1티어.canvas`·`2티어.canvas`** — 코어 업그레이드가 Notion 과 표현이 다르다.
- **`레시피.md`** — 2티어 절의 펌프·수경 재배기·업그레이드 모듈·아이템 강화기가 빈 줄이다.
- **게임 에셋** — 티타늄 주괴 ×4 (문서는 ×3 으로 통일함, §B-8).
- **Notion §2 자원 생성기 표** — 산출이 `암석`·`광석` 같은 옛 이름이다(정본은 `돌`·`마력석`).

---

## E. 2026-08-06 에 실제로 바뀐 것 (되돌릴 때 볼 목록)

**코드** — `ExtractionTable.cs`(2줄) · `ItemAliases.cs`(수동 분쇄기·탁한 우라늄 줄 교체, 반짝이는 돌 삭제) ·
`MachineAliases.cs`(임시 줄 제거) · `MachineBlockFiller.cs`(감별기 줄 제거)

**새 에셋 5** — `Blocks/Machines/ManualPulverizer.asset`·`.prefab` · `Items/Machines/ManualPulverizer.asset` ·
`Recipes/Incomplete/smelting/smelt_uranium.asset` · `Recipes/Incomplete/factory/uranium_concentrate.asset`

**삭제 18** — `shiny_stone` · `manual_crusher`(아이템) · Appraiser 3종 · `appraise_gems`×2 · `build_appraiser` ·
죽은 도구 조립 레시피 7 · 동일 중복 3 · 빈 `manual_extractor_t00`

**개명** — 레시피 에셋 45개(`craft_`/`build_` 37 · `_2` 5 · ToolRecipe 3) + `unrefined_uranium → turbid_uranium`.
`StreamingAssets/Recipes/*.json` 의 `id` 36개도 같이 바꿨고 지운 레시피 10개는 JSON 에서도 지웠다
— 안 하면 `Import JSON Recipes` 가 옛 이름으로 통째로 되살린다(`RecipeJsonImporter.cs:208`).

**수치** — Items 223→**221** · Recipe(ToolRecipe 포함) 170→**158** · MachineBlock **48**(감별기 −1, 수동 분쇄기 +1) ·
BlockBase **56** · 씬 `recipesList` **139**(변화 없음 — 지운 것들은 산출이 없어 원래 등록되지 않았다)

---

## F. 2026-08-06 오후 ~ 08-07 — 사용자가 Notion 을 직접 개정한 것

오전 반영이 끝난 뒤 사용자가 **Notion 과 `레시피.md` 를 직접 고쳤다.** 그 과정에서 규칙 하나가 못박혔다.

### F-0. ⚠ 기계에는 티어가 없다 (2026-08-07 확정)

> **"n티어 기계는 존재하지 않는다. 어떤 기계를 n티어에서 만들 수 있을 뿐이다.
>  따라서 같은 기계가 n티어에도 있고 m티어에도 있으면 안 된다."**

- Notion 공장 표의 **「현재 티어」 열은 해금 시점**이지 기계의 속성이 아니다.
- 코드의 티어는 **두 축**이다 — 해금(`건설 Recipe.tier` vs `CoreCrafter.tier`) ·
  처리(`MachineBlock.tier` vs `가공 Recipe.tier`). 자세한 것은 `CLAUDE.md` §4.
- 업그레이드는 **이름이 다른 별개 기계**로 표현한다 (`화로 → 전기로 → 고전압 전기로`).
- **이 규칙에 걸린 둘을 지웠다** (아래 F-1).

### F-1. 규칙 위반 제거 — 완료 (2026-08-07)

- [x] **`2티어 합금 재련기` 삭제** — 용광로 = `철근 ×50 + 합금 재련기 ×1 + 기계 강화 모듈 ×1`.
      강화 합금·특수합금은 (하나뿐인) 합금 재련기가 만든다. 에셋엔 원래 없어서 **문서만** 고쳤다
- [x] **`정유기` 삭제, 원유 처리를 `증류기`(2티어) 하나로** — 에셋에서도 지웠다:
      `refine_oil` 레시피 · `Machine:Refinery` 블록·프리팹·아이템 3종.
      `ItemAliases` 에 `정유기`·`Machine:Refinery` → `Machine:Distiller` 안전망 추가,
      `MachineBlockFiller` 의 `{정유기 → Refinery}` 줄 제거, JSON 2곳에서 `refine_oil` 제거
      → ⚠ **`석유`(petroleum)가 만들 길을 잃었다.** 증류기는 원유를 가스로 만든다.
        아이템은 남겨 뒀다(`마법 가루` 와 같은 처지). 쓸 곳이 정해지면 붙이면 된다

### F-2. 제련 규칙이 뒤집혔다 — **에셋 미반영**

오전에 내가 넣은 *"화로는 0티어라 tier 1 제련을 못 한다"* 를 사용자가 지우고
**"모든 티어의 화로는 모든 광석을 재련한다. 단, 티타늄만 용광로"** 로 바꿨다.

- [ ] `smelt_lead` · `smelt_nickel` · `smelt_tin` · `smelt_uranium` 의 `Recipe.tier` **1 → 0**
      (지금은 tier 0 화로가 못 돌린다)
- [ ] `blast_titanium` — 화로 ×4 → **용광로** ×3
- [ ] `blast_steel` — **용광로**, 철 주괴 ×2 **+ 석탄 ×1** (지금 화로에 붙어 있고 입력 0)

### F-3. 해금 축이 어긋난 것 — **에셋 미반영**

- [ ] `pulverizer`(전기 분쇄기) 건설 레시피 `tier` **0 → 1** (Notion 이 1티어 표로 옮겼다)
- [ ] **코어 업그레이드 3차 신설** — 핵발전소가 3티어로 갔는데 코어 표는 2차(→2티어)까지뿐이다

### F-4. 새 기계 5종 · 새 아이템 3종 — **에셋 없음**

| 기계 | 해금 | 건설 재료 |
|---|---|---|
| 전기 화로 | 0티어 | 벽돌 + 드라이버 + 철판 ×10 + 전도체 가루 ×4 |
| 상자 | 0티어 | 철판 ×10 + 돌 ×10 |
| 단일 저장소 | 1티어 | (빈칸) |
| 원유 펌프 | 2티어 | 펌프 + 컴퓨터 칩 + 철근 ×40 |
| 증류기 | 2티어 | 전기 화로 + 철근 ×70 (에셋 `Machine:Distiller` 는 있고 건설 레시피 `distiller` 도 t2) |

- [ ] 아이템 — **특수합금**(강철5 + 알루미늄5 + 마력파편50) · **강화 합금**(합금 재련기로 내려옴) ·
      **강철 주괴**(용광로, 철 주괴2 + 석탄1) + `레시피.md` 의 **고급 재단**(특수합금100 + 티타늄50)
- [ ] ⚠ 전기 화로가 **0티어 해금**인데 에셋 `ElectricFurnace` 의 `MachineBlock.tier` 는 1 —
      축이 달라 충돌은 아니지만, 건설 레시피가 없으므로 `tier 0` 으로 새로 만들어야 한다

### F-5. 개명 · 삭제 표시 — **에셋 미반영**

- [ ] `원유 채굴기`(1티어) → **`펌프`** ⚠ 에셋에 `Machine:Pump`·`Machine:OilDrill` 이 둘 다 있다
- [ ] 핵발전소 1티어 → **3티어** (철근 콘크리트 + 납 주괴 ×50 + 강화합금 ×200)
- [ ] Notion 이 레시피 칸에 `삭제` 라고 적은 3종 — `화약 제조기` · `유리 가공기` · `시멘트 공장` ·
      `아이템 강화기`. ⚠ **유리 가공기·시멘트 공장은 MachineBlock 도 레시피도 실재한다**
      (`glass_pipe` `glass_container` `acid_pipe` / `cement`) — 지우면 레시피도 함께

### F-6. 표기 · 수치 — **에셋 미반영**

- [ ] 합금 재련기가 **"광석·주괴 상관 없음" 을 버리고 주괴만** 받는다.
      산출도 `청동 주괴`·`인바 주괴` — 현재 에셋 표시명은 `청동`·`인바`·`강철`(주괴 없음)
- [ ] 화로 건설에서 **갈탄 ×1 제거** · 압연기 → 벽돌 + 드라이버 + 철판×8 + 전도체 가루×4 ·
      변압기 → 철근 + 금판×4 + 철판×4 + 구리판×4 · 전기 분해기·화학 처리기 재료 구체화
- [ ] 정밀 세공기 → 레이저 가공기×1 + **철판 ×50** · 수경 재배기 → 흙×20 + 특수합금×50 ·
      업그레이드 모듈 속도 = 특수합금×200 / 효율 = 강화합금×200
- [ ] 2티어 도구류 내구도 `무한` → `미정`

### F-7. 다음에 물어볼 것

1. **Notion 우라늄 주괴 비고가 아직 `1티어 (전기로)`** — F-2 의 새 규칙과 모순이다
2. **`삭제` 4종을 에셋에서도 지울지**
3. **`석유`(petroleum)** — F-1 로 만들 길이 없어졌다. 지울지, 증류기 산출로 되살릴지

---

## G. 3티어 (임시 기획 · 2026-08-07)

**설계 정본은 Notion 임시 페이지에 있다** — `🧪 3티어 (임시) — 저장 네트워크 + 핵발전소`
(`기획 - 테크트리` DB). 확정 전이라 `📔 기획 백로그 (수정본)` 에는 아직 안 합쳤다.

3티어 = **AE2 의 ME 시스템 같은 저장 네트워크 + 핵발전소**. 정한 것 셋 —
채널 제한 없이 **전력이 곧 한계**(컨트롤러40 + 드라이브20×3 + 터미널5 = 105/s vs 화력 20/s) ·
**이번엔 저장만**(자동 조합은 4티어) · **사용후 핵연료봉을 두고 화학 처리기가 재처리**.

### G-1. 왜 싸게 붙는가 (조사로 확인한 재사용 지점)

| AE2 | 재사용할 것 |
|---|---|
| ME 케이블 | `PipeKind` 에 값 하나 → `PipeRouter.Connects`·`ConnectionMask`·오토타일·`PipeNetworkManager` 렌더가 공짜 |
| Import/Export Bus | **렌치 면 상태**(`Insert`/`Extract`) 그대로 → 새 기계도 새 UI도 없다 |
| 서브넷 분리 | 렌치 `Cut` |
| 셀 내용(NBT) | `StorageCellInstance : ItemInstance` → **세이브 버전 상승 불필요**(v4 부터 슬롯별 ItemInstance) |
| ME 터미널 | `IItemContainer`(`UI/Slot/IItemContainer.cs`) 가상 구현 → `ItemSlot` 드래그앤드롭 그대로 |
| 채널 예산 | 안 만든다. `PowerLinkMode` + `energyUseRate` |

`PlaceableRecord` 새 필드 0 · 신규 파일 4개(`StorageCellInstance` `StorageNetwork`
`NetworkContainer` `StorageTerminalUI`) · 기존 수정은 `PipeKind` 값 1개 + `MachineInstance` 훅 정도.

### G-2. 구현 전에 반드시 처리할 것

- [ ] `ChemicalProcessor` 출력 1 → **2** (재처리가 우라늄 농축물 + 납 주괴 둘을 낸다)
- [ ] `PrecisionLathe` 입력 1 → **3** (핵연료봉이 재료 3종)
- [ ] `NuclearPlant` 출력 0 → **1** (사용후 봉 자리)
- [ ] `TickGenerator` 에 **산출 훅** — 연료를 다 태우면 사용후 봉을 내보내고, 자리가 없으면 멈춘다
- [ ] ⚠ **3티어 신규 제작은 전부 코어 조합기에 `Recipe.tier = 3`**.
      고급 조합기는 처리 티어가 1 이라 3티어 레시피를 두면 목록에 안 뜬다 (§F-0 의 두 축 문제)

### G-3. 선행 조건 — 3티어는 2티어 위에 선다

- [ ] **특수합금 · 강화 합금** 아이템 신설 (§F-4) — 공명 칩·저장 기판·컨트롤러·핵발전소가 전부 먹는다
- [ ] **코어 업그레이드 1·2차 구현** (§A-5) — 3차(공명 칩)를 얹을 자리
- [ ] **정밀 세공기 · 화학 처리기 건설 레시피** (§C-5·C-6) — 둘 다 없거나 입력 0
- [ ] **우라늄 농축물 사슬이 실제로 도는지** (§F-3) — 전기로 건설 레시피가 없어 우라늄 주괴를 못 만든다

### G-4. 확정되면 할 것

- [ ] Notion 임시 페이지를 `📔 기획 백로그 (수정본)` 에 합친다
      (3티어 표 신설 · 핵발전소를 2티어 표에서 옮김 · 코어 업그레이드 3차 행 · 자원 목록에 새 아이템 10종)
- [ ] `레시피.md` 에 `# 3티어` 절 신설
