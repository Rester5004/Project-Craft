# TODO — 테크트리 실현 가능성 검토

> 조사일 **2026-08-05** · 정본 3종(Notion `기획 백로그(수정본)` · Obsidian 캔버스 6장 · `레시피.md`)과
> 현재 프로젝트(레시피 170 · 아이템 223 · 블록 56)를 전수 대조한 결과.
>
> **결론: 지금 상태로는 0티어에서 막혀 아무 데도 갈 수 없다.** 막는 것은 아래 §B 의 P0 네 가지다.

**이번에 확정한 것**

| 항목 | 결정 |
|---|---|
| 추출 산출 배정 | **캔버스를 정본으로 한다** (현재 구현된 레시피 9개 + `ExtractionTable` 이 그대로 정본) |
| 2티어 수치 | **`레시피.md` 값을 그대로** 쓴다. 거기에도 없는 칸만 `(미정)` |

> ## 2026-08-06 반영 — 결정 8건을 에셋·코드에 넣었다
>
> **✅ 해결됨** — A-1(유황석·미가공 우라늄·반짝이는 돌) · A-4(0티어 부트스트랩) ·
> B-1(도구 레시피 두 벌) · B-3(`build_extractor` 빈 산출) · B-5 일부(동일 중복 3쌍) ·
> C-4(감별기 — 삭제로 종결) · C-8 일부.
> **A-2 · A-3 · A-5 · A-6 은 그대로 열려 있다**(A-3·A-5·A-6 은 "추후"로 미루기로 결정).
> 자세한 내용은 각 절의 취소선과 §E 를 볼 것.

---

## 0. 용어 못박기

세 문서가 같은 말을 다르게 써서 생긴 오해가 많았다. 아래를 기준으로 한다.

- **`n-m티어 추출기`** — **`n` = 들어가는 자원의 계열**(0=돌 · 1=마력석 · 2=운석),
  **`m` = 추출기 등급**. 같은 계열에서 m 이 오를수록 산출 종류가 늘어난다(슈퍼셋).
- **`m` 은 해금 시점과 별개다.** `n=2 m=0` 추출기가 테크상 3티어에 가서야 열려도 이상하지 않다.
  → 등급표(`ExtractionTable`)와 **해금표(건설 레시피)는 따로 정해야 한다.**
- **구조는 `분쇄기 → 추출기`.** 분쇄기는 주 산출물만 100% 로 내고, **부산물은 추출기가 확률로** 낸다.
  Notion 의 "분쇄/추출 테이블"이 둘을 한 장에 합쳐 놓아 분쇄기가 부산물을 내는 것처럼 읽힌다.
- ⚠ **Notion §2 의 "자원 생성기 = 지형 기반 자동 추출"은 추출기가 아니다.**
  프로젝트의 `자원 생성기`·`펌프`·`지열 발전기` 쪽이다. 같은 문단에 있어 계속 혼동을 부른다.

---

## A. 먼저 정해야 할 것

레시피를 만들기 전에 답이 필요한 것들. **정하지 않고 만들면 또 갈아엎게 된다.**

### ~~A-1. 유황석 · 반짝이는 돌 · 미가공 우라늄을 어디서 낼 것인가~~ ✅ 2026-08-06 해결

**0계열 추출을 가공 전 단계로 바꿔서 풀었다** — 1계열에 얹는 권장안 대신, 0계열이 이미 내던
`황 가루`·`우라늄 가루`(완제품이라 그 뒤 기계를 죽이던 것)를 원료로 되돌렸다.

| 무엇을 | 어떻게 |
|---|---|
| 유황석 | `extract_stone_powder` 의 `황 가루 5%` → **`유황석 5%`**(0-2 개방). 화학 처리기 3레시피가 살아났다 |
| 탁한 우라늄 | 같은 곳의 `우라늄 가루 1%` → **`탁한 우라늄 1%`**(0-3 개방). `미가공 우라늄` 을 개명해 재사용 |
| 반짝이는 돌 | **삭제.** 감별기까지 함께 지웠다 — 루비·사파이어·다이아몬드는 1계열 추출이 이미 낸다 |

새 우라늄 사슬 — `탁한 우라늄` →화학(+산성 용액 ×2)→ `우라늄 조각` →전기로 `smelt_uranium`→ `우라늄 주괴`
→압축기 `uranium_concentrate`(×10)→ `우라늄 농축물`. 뒤의 두 레시피는 이번에 새로 만들었다.

### A-2. 추출기 12종의 해금 시점 🔴

`m` 은 등급일 뿐이라 **언제 만들 수 있는지가 어디에도 없다.**
현재 추출기 13종 중 **건설 레시피가 있는 것은 `0-1티어` 하나뿐**이다.

- **(권장) 계열 해금과 등급 업그레이드를 분리** — `n` 계열은 그 메인자원에 닿을 때 열리고(0=시작, 1=지하층,
  2=외곽), `m` 은 기존 추출기 + 부품으로 업그레이드(레시피.md 의 `0-2티어 = 컴퓨터 칩2 + 0-1티어 + 니켈 주괴1`
  패턴). 표 하나로 12칸을 채울 수 있다.
- (대안) 12종 각각 독립 레시피 — 자유롭지만 표가 12줄로 늘고 서로 어긋나기 쉽다.

### ~~A-3. 마력 파편의 최초 획득처~~ ✅ 2026-08-10 해결 — **지하맵 전리품**

`UndergroundLootTable` 에 `mana_shard`(25% · 1~3개)와 `iron_ingot`(20% · 1~4개)이 들어가면서
0티어 마법과 0티어 부트스트랩이 함께 열렸다. 아래는 당시 기록.

---


**어느 문서에도 없다.** 있는 것은 증식뿐이다 — `마력 파편1 + 구리 주괴10 → 마력 파편2`.
0에서 시작할 수 없으므로 **0티어 마법 전체와 1티어 진입이 통째로 막힌다**
(초급 재단 · 물 · 용암 · 자원 생성기 · 마법이 부여된 전도체 가루).
**수동 0-0티어 추출기(`돌10 + 크랭크 + 마력파편`)도 여기 걸려 아직 못 만든다.**

- **(권장) 마력석에서 낸다** — `0티어.canvas` 의 "지하층에서 마력광석 획득 → 기초 마법 시작"과 맞는다.
  마력석 분쇄/추출 산출에 마력 파편을 넣거나, 마력석 → 마력 파편 조합대 레시피.
- (대안) 월드 드랍 / 시작 지급 — 지하층 진입 전에 마법을 열고 싶을 때.

### ~~A-4. 0티어 부트스트랩을 어떻게 풀 것인가~~ ✅ 2026-08-06 해결

**수동 분쇄기를 값싸게 만들고 옛 재료를 전기판으로 내려서 풀었다.**

```
수동 분쇄기 = 돌 10 + 크랭크 1        크랭크 = 돌 2 (칼 필요)  ← 순환 없음. 여기서 시작한다
전기 분쇄기 = 크랭크 1 + 철 주괴 2 + 철판 2 + 돌 4   ← 옛 수동 레시피를 그대로 물려받았다
0-0티어 추출기(전기) = 크랭크 1 + 구리 주괴 2 + 구리판 2 + 돌 4  ← 옛 수동 추출기 재료
수동 0-0티어 추출기  = 돌 10 + 크랭크 1 + 마력 파편 1  ← A-3 이 풀려야 만들 수 있다
```

`수동 분쇄기` 는 플레이스홀더 아이템이던 것을 **정식 기계 `Machine:ManualPulverizer` 로 승격**했다
(tier 0 · `recipeGroupId = Pulverizer` · `manualStepRatio 0.05` · 무전력).
`MachineAliases` 의 임시 줄 `{수동 분쇄기 → 전기 분쇄기}` 는 이때 지웠다.
⚠ 벽돌은 이제 어느 0티어 건설 레시피에도 안 쓰인다 — `화로`·`압축기`만 여전히 요구한다.

### A-5. 코어 업그레이드를 무엇으로 구현할 것인가 🟡 — **코어 조합기 UI 에 붙일 예정 (2026-08-06 결정)**

캔버스에는 1차·2차가 있는데 **Notion 에도 프로젝트에도 없다.** `CraftingTableBlock.tier` 로 레시피를 거르지만
**티어를 올리는 수단이 없다.**

- 캔버스: 1차 = `마법이 부여된 전도체 가루` ×1 · 2차 = `마력 칩`
- **(권장) 코어 블록 교체 레시피** — 기존 기계 업그레이드와 같은 방식(`중급 재단 = 초급 재단 + 금 주괴10`).
  `tier` 가 다른 `CraftingTableBlock` 을 하나 더 만들고 그것으로 바꾼다. 코드 변경 0.
- (대안) 전용 상호작용 — 코어에 아이템을 넣으면 티어가 오른다. 새 코드가 필요하다.

### A-6. 전압 2풀을 도입할 것인가 🟡 — **추후 구현 (2026-08-06 결정: 미룬다)**

Notion 은 저전압/고전압 2풀이고 "전기 분해기는 고전압 필수"인데, **현재는 단일 풀**이라 표현되지 않는다.

- (권장) **미룬다** — 지금 막힌 것들(A-1~A-4)과 무관하고, `MachineInstance`·`PowerLinkMode`·세이브까지
  건드려야 한다. 0·1티어가 도는 것을 먼저 확인하고 손대는 게 낫다.
- (대안) 지금 넣는다 — 2티어 설계가 전압 전제라면 나중에 더 비싸진다.

---

## B. 결정과 무관하게 지금 고쳐야 할 결함

### 🔴 P0 — 이것 때문에 게임이 안 돈다

**~~B-1. 망치·드라이버·곡괭이를 만들 수 없다~~** ✅ 2026-08-06 정정 + 해결

⚠ **이 항목의 진단이 틀렸었다.** `Craft_Hammer`·`Craft_Driver`·`Craft_Pickaxe` 는 일반 `Recipe` 가 아니라
**`ToolRecipe`** 다 — 재료가 구체적 아이템이 아니라 **부품 종류**라서 `inputs` 가 비어 있는 것이 **정상**이고,
조합대가 부품 칸을 따로 띄운다. 즉 이쪽이 정본이고 정상 동작한다.

실제 고장은 반대쪽이었다: 재질을 고정한 옛 조립 레시피 7개(`hammer` `stone_hammer` `iron_hammer`
`craft_stone_hammer` `craft_iron_hammer` `driver` `screwdriver`)를 `ToolAssetGenerator` 가 산출 참조만
끊어 놓고 남겨 둔 것. **전부 지웠고** ToolRecipe 3개를 `hammer`·`driver`·`pickaxe` 로 개명했다.

부품 레시피는 원래부터 정상이었다 — `stick`(돌1 → 나무 자루) · `hammer_head`(돌2 → 나무 망치 머리) ·
`stone_hammerhead` · `iron_hammerhead`. **망치·드라이버는 이제 만들 수 있다.**
⚠ **곡괭이는 여전히 못 만든다** — `*_pickaxe_head` 16재질 전부 레시피가 없다(§C-9).

**B-2. 판재를 철판·실리콘 판밖에 못 만든다** → 컴퓨터 칩(구리판 ×10)이 막히고,
그 뒤의 **레이저 가공기 · 자동 조합기 · 마력 칩 · 0-2티어 추출기가 전부 죽는다.**
(§C-1 에서 만든다)

**~~B-3. `build_extractor` 의 산출이 삭제된 에셋~~** ✅ 2026-08-06 — `Machine:Extractor00Plus` 로 채웠고
에셋 이름도 `extractor00plus` 로 바꿨다.

**~~B-4. 2티어 건설 레시피 11개가 입력 0~~** ✅ 2026-08-12 — **기계 10개는 전부 채웠다**(§K).
노션 2티어 표의 값 + 티어 기본 재료 `철근 콘크리트 ×2`. 막고 있던 `강화 합금`·`기계 강화 모듈` 도 이때 만들었다.
⚠ 남은 둘은 기계가 아니라 **아이템**이다 — `upgrade_module_speed`(특수합금 200) ·
`upgrade_module_efficiency`(강화합금 200) 는 아직 입력이 비어 있다.

### 🟡 P1 — 조용히 어긋나 있는 것

**B-5. 중복 레시피** — `SelectRecipe` 가 첫 것만 쓰므로 뒤쪽은 죽은 레시피다.
2026-08-06 에 7쌍을 정리했다(취소선). 남은 것:

```
[초급 재단]    magic_lava / magic_water          ← 입출력이 같아 물과 용암을 구분할 수 없다 ⚠
[핵발전소]     nuclear_power / power_nuclear
[CoreCrafter vs 제단] apple_tree_seed · enchanted_conductor_powder · enchanter ·
                      resource_generator_t0 · resource_generator_t0_enhanced   ← 의도된 이중 경로인지 확인 필요
~~[코어 조합기] driver / screwdriver · craft_iron_hammer / iron_hammer · craft_stone_hammer / stone_hammer~~ ✅ 삭제
~~[감별기] appraise_gems / appraise_gems_2~~ ✅ 감별기째 삭제
~~[전기 분해기] electrolyzer_water ×2 · [마나 용해기] mana_from_crystal ×2 · mana_from_shard ×2~~ ✅ 하나씩만 남김
```

`magic_lava`/`magic_water` 는 단순 중복이 아니라 **설계 결함**이다 — 재료가 같아
(양동이 + 마력 파편2) 플레이어가 물과 용암을 고를 수 없다. 재료를 다르게 하거나 기계를 나눠야 한다.

**B-6. 발전기 레시피 10개가 전부 무의미하다** — 발전은 `MachineInstance.TickGenerator` 가 연료 슬롯에서
직접 태우므로 레시피를 보지 않는다. 화력 발전기 7개 · 핵발전소 2개 · 지열 1개 전부 죽은 에셋이다.
`석탄 or 갈탄` · `석탄 또는 갈탄` · `석탄/갈탄/석유 중 1` · `석탄 / 갈탄 / 석유` 같은
**"이 중 아무거나" 가짜 아이템 4종**도 여기서 나온 것이라 같이 정리 대상이다.

**B-7. 이름이 내용과 다르다**
- `craft_stick` : **돌 1개 → 나무 막대** / `hammer_head` : **돌 2개 → 나무 망치 머리** — 돌로 나무를 만든다
- `blast_steel`(강철)이 **화로**에 붙어 있다 — Notion·`레시피.md` 는 **용광로** 담당
- 1계열 사슬 레시피 이름이 옛 아이템 이름 그대로다 — `crush_ore` `crush_crushed_ore` `crush_ore_grains` ·
  `extract_crushed_ore` `extract_ore_grain` `extract_shiny_powder`
  (아이템은 `마력석 조각/가루/미세한 마력석 가루` 로 바뀌었다)

**B-8. 수량이 문서마다 다르다** — 티타늄 주괴가 Notion ×4 / `레시피.md` ×3. **현재 에셋은 ×4.**
`레시피.md` 우선으로 정했으므로 **에셋과 Notion 을 ×3 으로 맞춘다.**

### 🟢 P2 — 미구현이라 비어 있는 것 (버그 아님, 기능이 없을 뿐)

마나 용해기(유체 미구현) · 변압기(전압 체계 미구현) · 전기 분해기의 물 → 수소/산소(기체 미구현)
(~~감별기 확률 분류~~ — 감별기는 삭제됐다)

---

## C. 만들어야 할 레시피

### C-1. 판재 — **P0** (없으면 컴퓨터 칩 이후가 전부 막힌다)

압축기에 `철판`·`실리콘 판`만 있었다. 나머지를 만든다. (압연기는 압축기로 통합됐다 — §J)

- [ ] 구리판 ← 구리 주괴 ×1 (압축기)
- [ ] 금 판 ← 금 주괴 ×1
- [ ] 은 판 ← 은 주괴 ×1
- [ ] 청동 판 ← 청동 ×1
- [ ] 인바 판 ← 인바 ×1
- [ ] 금속 판 ← 금속 주괴 ×1 — `compressor_metal_plate` 가 이미 있으나 **`금속 주괴` 자체를 못 만든다**.
      `금속`/`금속 주괴` 가 "아무 금속이나"를 뜻하는 가짜 아이템인지 실제 아이템인지 먼저 정할 것

### C-2. 0티어 부트스트랩 — 대부분 해결

- [x] 첫 기계 경로 — **수동 분쇄기 = 돌 10 + 크랭크 1** (2026-08-06). 철도 벽돌도 필요 없다
- [x] 분쇄기 건설 레시피에서 순환 제거 — 전기판이 옛 재료(크랭크+철+철판+돌)를 물려받았다
- [ ] 마력 파편 최초 획득 경로 (A-3) — **추가 예정**. 이게 없으면 0티어 마법과 수동 추출기가 막힌다
- [ ] 손 채굴로 첫 금속을 줄지 — 지금은 분쇄기 → 추출기로만 철을 얻는다(경로는 뚫려 있음)

### ~~C-3. 1계열 부산물 3종~~ ✅ 2026-08-06 — 0계열 산출을 가공 전 형태로 바꿔 해결 (§A-1)

### ~~C-4. 감별기 확률 분류~~ ✅ 2026-08-06 — 감별기·반짝이는 돌·레시피 2개를 모두 삭제

### C-5. 건설 레시피 — **P1** (기계는 있는데 만들 방법이 없는 것)

추출기 (해금 시점은 A-2 결정에 따름)
- [x] 0-0티어(수동) `manual_extractor_t00` · 0-0티어(전기) `extractor00plus` — 2026-08-06
      (수동 쪽은 마력 파편이 없어 실제로는 아직 못 만든다 — A-3)
- [ ] 0-3티어 `extractor03` — 에셋은 있으나 입력 0 (§C-6)
- [ ] 1-0 · 1-1 · 1-2 · 1-3티어
- [ ] 2-0 · 2-1 · 2-2 · 2-3티어
- [x] 0-1티어 `extractor01` · 0-2티어 `extractor02` — 원래 있었고 이름만 정리

그 밖 기계 9종 — 전부 `철근 콘크리트 + 부품` 패턴(Notion 1티어 공장 표)
- [ ] 변압기 · 화학 처리기 · 시멘트 공장 · 유리 가공기 · 원유 채굴기 · 정유기 · 지열 발전기 · 핵발전소
- [ ] 전기로(1티어 화로) · 고전압 전기로(2티어 화로) — 화로 업그레이드 경로
      ⚠ **전기로가 없으면 tier 1 제련(납·니켈·주석·우라늄)을 못 한다** — 화로는 tier 0 이라 목록에서 걸러진다

### C-6. 2티어 — **P2** (`레시피.md` 값 그대로)

- [ ] 정밀 세공기 = 레이저 가공기 ×1 + 철근 ×5 + `(미정)`
- [ ] 2티어 합금 재련기 = 1티어 합금 재련기 ×1 + 기계 강화 모듈 ×1
- [ ] 용광로 = 철근 ×50 + 2티어 합금 재련기 ×1 + 기계 강화 모듈 ×1
- [ ] 고급 조합기 = 티타늄 주괴 ×100 + 컴퓨터 칩 ×20 + 마력 결정 ×20 + 정밀 세공기 ×1
- [ ] 가스 발전기 = 철근 ×20 + 강화 합금 ×20
- [ ] 0-3티어 추출기 = 0-2티어 추출기 ×1 + 강화 합금 ×10 + 기계 강화 모듈 ×1
- [ ] 강화 합금 ×5 = 인바 주괴 ×5 + 청동 주괴 ×5 + 철 주괴 ×5 (2티어 합금 재련기)
- [ ] 기계 강화 모듈 = 마력 결정 ×20 + 철근 ×10 (정밀 세공기)
- [ ] 모터 / 베어링 / 프로펠러 = 강화 합금 ×10 (정밀 세공기) — 지금 입력 0
- [ ] 강철 주괴 ×1 = 철 주괴 ×2 (용광로) — 지금 `blast_steel` 이 화로에 있고 입력 0
- [ ] 티타늄 주괴 ×1 = 티타늄 조각 ×3 (용광로) — 지금 화로에 ×4
- [ ] 펌프 · 증류기 · 수경 재배기 · 업그레이드 모듈 2종 · 아이템 강화기 = **`(미정)`**

### C-7. 새로 만들어야 할 아이템

- [x] **강화 합금** — ✅ 2026-08-12. 합금 재련기 t1 · `인바 ×5 + 청동 ×5 + 철 주괴 ×5 → ×5`
- [x] **기계 강화 모듈** — ✅ 2026-08-12. 정밀 세공기 t2 · `마력 결정 ×20 + 철근 ×10 → ×1`.
      이때 정밀 세공기 입력칸을 1 → 2 로 늘렸다(재료 2종이 안 들어갔다)
- [x] ~~2티어 합금 재련기~~ — 만들지 않는다. "같은 기계가 두 티어에 있으면 안 된다" 규칙으로 2026-08-07 에 폐기(§F)
- [ ] `강철 주괴` — 지금 아이템 이름은 `강철`(steel)이다. 주괴 표기로 통일할지 정할 것 (§B-7)
- [ ] 나무 씨앗 (`apple_tree_seed` 의 재료인데 만들 방법이 없다)
- [x] 우라늄 주괴 — `smelt_uranium`(우라늄 조각 ×4 → ×1, 전기로) 추가 (2026-08-06)
- [ ] 오스뮴 주괴 · 토륨 주괴 — 조각은 나오는데 **제련 레시피가 없다**

### C-8. 정리 (만드는 게 아니라 지우는 것)

- [x] 동일 중복 3쌍 · 죽은 도구 조립 레시피 7개 삭제 (2026-08-06, §B-5)
- [ ] 발전기 죽은 레시피 10개 (§B-6) · 가짜 연료 아이템 4종
- [ ] `magic_lava`/`magic_water` 재료 구분 · `nuclear_power`/`power_nuclear` 중복
- [ ] 도구 부품 재질별 미사용 에셋 40여 종 — `ToolAssetGenerator` 가 만들었으나 레시피가 없다.
      도구 체계를 정식화할 때 같이 정할 것 (지금 지우면 되살리기 번거롭다)

### C-9. 곡괭이를 만들 수 없다 — **P1** (B-1 에서 새로 드러난 것)

`Craft_Pickaxe`(→ `pickaxe`) ToolRecipe 는 정상인데 **부품이 없다.**

- [ ] `*_pickaxe_head` — 16재질 전부 레시피가 없다 (망치 머리는 나무·돌·철 3종이 있다)
- [ ] `*_rod` — `wood_rod`(`stick`) 하나뿐. `stone_rod`·`iron_rod` 등이 없다

---

## D. 정본 문서 정리

### D-1. Notion 에 반영한 것 (2026-08-05 완료)

대상: `📔 기획 백로그 (수정본)` — 부분 치환으로만 고쳤고, 수정 전 원문은 스크래치패드에 백업했다.
**절이 삭제되거나 표 구조가 깨진 곳은 없다**(수정 전후 절 제목 목록 대조 완료).

1. 0/1/2계열 분쇄·추출 테이블의 부산물을 **캔버스 값**으로 교체. 열리는 등급 `(m0~m3)` 을 함께 표기
2. 주 산출물 열의 옛 이름 → 새 이름 (마력석 조각/가루/미세한 마력석 가루 · 운석 조각/가루/미세 가루).
   0계열도 `가루` → `돌 가루` 로 명시
3. mermaid 3개를 표와 맞춤 — 예전엔 표보다 한 칸 어긋나 있었고 메인자원 노드가 빠져 있었다
4. "추출기 티어 구조" → **"추출기 구조: `n-m` — 계열 × 등급"** 으로 다시 씀.
   n·m 정의, 슈퍼셋, **해금 시점은 m 과 별개**, 등급별 속도·확률 배수표
5. §2 첫머리에 **"자원 생성기는 추출기와 다른 기계다"** 경고 추가
6. 자원 목록의 티어·획득 방법을 캔버스 배정으로 (금속 11종 · 비금속 7종).
   출처가 사라진 것(유황석·반짝이는 돌·미가공 우라늄)은 `⚠ 출처 미정 (A-1)` 로 표시
7. 2티어 공장 표를 `레시피.md` 값으로 채움. 없는 칸은 `(미정)`.
   `2티어 업그레이드 모듈` → **`기계 강화 모듈`** 로 이름 정정
8. **코어 업그레이드** 표 신설 (1차 = 마법이 부여된 전도체 가루 ×1 · 2차 = 마력 칩 ×1)
9. 티타늄 주괴 ×4 → **×3**, 담당 기계를 화로 → **용광로**로 (제련 표·주괴 표 두 곳)
10. 최근 변경 요약 callout 에 이번 갱신 8줄 추가 + **"검토 결과는 저장소 TODO.md 에 있다"** 안내

곁들여 고친 것 — `마법 가루`가 갈 곳을 잃었다는 표시, `나무 씨앗` 획득 경로 부재 표시,
`반짝이는 가루` → `미세한 마력석 가루` 로 행 갱신

### D-2. 2026-08-06 변경을 정본 문서에 반영 (완료)

**Notion `📔 기획 백로그 (수정본)`** — 부분 치환만 사용, 수정 전후 재-fetch 대조 완료
1. 0계열 표·mermaid 의 `황 가루` → `유황석`, `우라늄 가루` → `탁한 우라늄`.
   mermaid 에 화학 처리기 후속 노드 2개 추가 + "추출 산출은 가공 전 형태로" 경고문
2. 자원 목록 — `미가공 우라늄` 행을 `탁한 우라늄`(0계열 m3 1%)으로, `우라늄 조각` 행 신설.
   `유황석` 은 `⚠ 출처 미정` → `돌 가루 추출 (m2) 5%`
3. 주괴 표 — `우라늄 주괴`(조각 ×4, 전기로) 신설, `우라늄 농축물` 을 압축기(주괴 ×10)로
4. 보석류 표 — `반짝이는 돌` 행 삭제, 루비·사파이어·다이아몬드를 1·2계열 추출 출처로
5. 0티어 공장 표 — 분쇄기/추출기 행을 **수동·전기 4행**으로 분리, `감별기` 행 삭제
6. 제련 표 제목을 `(화로 · 전기로)` 로, ~~"화로는 0티어라 tier 1 제련을 못 돌린다" 경고~~ + 우라늄 주괴 행
   (⚠ 이 경고는 **§F 에서 사용자가 뒤집었다** — 화로는 티어 무관, 티타늄만 용광로)
7. 가공 레시피 표 — 감별기 행 삭제, 화학 처리기 우라늄 행 수정, 압축기 행 추가
8. 인프라 규칙의 "0티어는 전력을 쓰지 않는다" → 수동/전기판이 따로 있다는 설명으로
9. callout 에 2026-08-06 요약 8줄

**Obsidian** — 수정 전 원본은 스크래치패드에 백업
- `자원과 그 가공방식.canvas` : `황 가루(5%)` → `유황석(5%)`. (`탁한 우라늄(1%)` 은 이미 반영돼 있었다)
  + 가공 전 형태 원칙과 후속 사슬을 적은 메모 노드 1개 추가 (99 → 100 노드)
- `레시피.md` : 0티어 절의 수동 분쇄기·수동 추출기 재료 교체(+ 전기판 2줄 신설),
  없어진 돌덩이 3종을 자갈/모래/돌 가루로, 1티어에 화학 처리기·전기로·압축기 우라늄 사슬 추가

### D-3. 아직 어긋난 채 남은 것

- **`1티어.canvas`·`2티어.canvas`** — 코어 업그레이드가 Notion 과 표현이 다르다.
- **`레시피.md`** — 2티어 절의 펌프·수경 재배기·업그레이드 모듈·아이템 강화기가 빈 줄이다.
- **게임 에셋** — 티타늄 주괴 ×4 (문서는 ×3 으로 통일함, §B-8).
- **Notion §2 자원 생성기 표** — 산출이 `암석`·`광석` 같은 옛 이름이다(정본은 `돌`·`마력석`).

---

## E. 2026-08-06 에 실제로 바뀐 것 (되돌릴 때 볼 목록)

**코드** — `ExtractionTable.cs`(2줄) · `ItemAliases.cs`(수동 분쇄기·탁한 우라늄 줄 교체, 반짝이는 돌 삭제) ·
`MachineAliases.cs`(임시 줄 제거) · `MachineBlockFiller.cs`(감별기 줄 제거)

**새 에셋 5** — `Blocks/Machines/ManualPulverizer.asset`·`.prefab` · `Items/Machines/ManualPulverizer.asset` ·
`Recipes/Incomplete/smelting/smelt_uranium.asset` · `Recipes/Incomplete/factory/uranium_concentrate.asset`

**삭제 18** — `shiny_stone` · `manual_crusher`(아이템) · Appraiser 3종 · `appraise_gems`×2 · `build_appraiser` ·
죽은 도구 조립 레시피 7 · 동일 중복 3 · 빈 `manual_extractor_t00`

**개명** — 레시피 에셋 45개(`craft_`/`build_` 37 · `_2` 5 · ToolRecipe 3) + `unrefined_uranium → turbid_uranium`.
`StreamingAssets/Recipes/*.json` 의 `id` 36개도 같이 바꿨고 지운 레시피 10개는 JSON 에서도 지웠다
— 안 하면 `Import JSON Recipes` 가 옛 이름으로 통째로 되살린다(`RecipeJsonImporter.cs:208`).

**수치** — Items 223→**221** · Recipe(ToolRecipe 포함) 170→**158** · MachineBlock **48**(감별기 −1, 수동 분쇄기 +1) ·
BlockBase **56** · 씬 `recipesList` **139**(변화 없음 — 지운 것들은 산출이 없어 원래 등록되지 않았다)

---

## F. 2026-08-06 오후 ~ 08-07 — 사용자가 Notion 을 직접 개정한 것

오전 반영이 끝난 뒤 사용자가 **Notion 과 `레시피.md` 를 직접 고쳤다.** 그 과정에서 규칙 하나가 못박혔다.

### F-0. ⚠ 기계에는 티어가 없다 (2026-08-07 확정)

> **"n티어 기계는 존재하지 않는다. 어떤 기계를 n티어에서 만들 수 있을 뿐이다.
>  따라서 같은 기계가 n티어에도 있고 m티어에도 있으면 안 된다."**

- Notion 공장 표의 **「현재 티어」 열은 해금 시점**이지 기계의 속성이 아니다.
- 코드의 티어는 **두 축**이다 — 해금(`건설 Recipe.tier` vs `CoreCrafter.tier`) ·
  처리(`MachineBlock.tier` vs `가공 Recipe.tier`). 자세한 것은 `CLAUDE.md` §4.
- 업그레이드는 **이름이 다른 별개 기계**로 표현한다 (`화로 → 전기로 → 고전압 전기로`).
- **이 규칙에 걸린 둘을 지웠다** (아래 F-1).

### F-1. 규칙 위반 제거 — 완료 (2026-08-07)

- [x] **`2티어 합금 재련기` 삭제** — 용광로 = `철근 ×50 + 합금 재련기 ×1 + 기계 강화 모듈 ×1`.
      강화 합금·특수합금은 (하나뿐인) 합금 재련기가 만든다. 에셋엔 원래 없어서 **문서만** 고쳤다
- [x] **`정유기` 삭제, 원유 처리를 `증류기`(2티어) 하나로** — 에셋에서도 지웠다:
      `refine_oil` 레시피 · `Machine:Refinery` 블록·프리팹·아이템 3종.
      `ItemAliases` 에 `정유기`·`Machine:Refinery` → `Machine:Distiller` 안전망 추가,
      `MachineBlockFiller` 의 `{정유기 → Refinery}` 줄 제거, JSON 2곳에서 `refine_oil` 제거
      → ⚠ **`석유`(petroleum)가 만들 길을 잃었다.** 증류기는 원유를 가스로 만든다.
        아이템은 남겨 뒀다(`마법 가루` 와 같은 처지). 쓸 곳이 정해지면 붙이면 된다

### F-2. 제련 규칙이 뒤집혔다 — **에셋 미반영**

오전에 내가 넣은 *"화로는 0티어라 tier 1 제련을 못 한다"* 를 사용자가 지우고
**"모든 티어의 화로는 모든 광석을 재련한다. 단, 티타늄만 용광로"** 로 바꿨다.

- [ ] `smelt_lead` · `smelt_nickel` · `smelt_tin` · `smelt_uranium` 의 `Recipe.tier` **1 → 0**
      (지금은 tier 0 화로가 못 돌린다)
- [ ] `blast_titanium` — 화로 ×4 → **용광로** ×3
- [ ] `blast_steel` — **용광로**, 철 주괴 ×2 **+ 석탄 ×1** (지금 화로에 붙어 있고 입력 0)

### F-3. 해금 축이 어긋난 것 — **에셋 미반영**

- [ ] `pulverizer`(전기 분쇄기) 건설 레시피 `tier` **0 → 1** (Notion 이 1티어 표로 옮겼다)
- [ ] **코어 업그레이드 3차 신설** — 핵발전소가 3티어로 갔는데 코어 표는 2차(→2티어)까지뿐이다

### F-4. 새 기계 5종 · 새 아이템 3종 — **에셋 없음**

| 기계 | 해금 | 건설 재료 |
|---|---|---|
| ~~전기 화로~~ | 0티어 | 벽돌 + 드라이버 + 철판 ×10 + 전도체 가루 ×4 — **✅ 완료** (`electric_furnace`) |
| ~~상자~~ | 0티어 | 철판 ×10 + 돌 ×10 — **✅ 완료** (`Machine:Chest`, 40칸) |
| ~~단일 저장소~~ | 1티어 | 상자 ×1 + 압축기 ×1 + 철판 ×10 — **✅ 완료** (`Machine:ItemStorage`, 1칸 1024 +1024/모듈) |
| 원유 펌프 | 2티어 | 펌프 + 컴퓨터 칩 + 철근 ×40 |
| 증류기 | 2티어 | 전기 화로 + 철근 ×70 (에셋 `Machine:Distiller` 는 있고 건설 레시피 `distiller` 도 t2) |

- [ ] 아이템 — **강화 합금**(합금 재련기로 내려옴) + `레시피.md` 의 **고급 재단**(특수합금100 + 티타늄50)
      (**특수합금**·**강철**은 ✅ — 강철은 `blast_steel` 로 용광로에 들어갔다)

### F-4b. 2026-08-10 테크 뚫기 — **완료**

새 월드가 시작조차 안 되고 코어 2티어에 못 가던 것을 아래로 풀었다.
지하맵(다우징 로드 → 포탈 → 물·마력 파편·광석 주괴)이 **철 주괴를 주는 것이 전제**다.

- [x] **부품 제작을 재질 슬롯 방식으로** — `ToolPartRecipe`(`rod`·`hammer_head`·`pickaxe_head`) +
      `ToolMaterial.sourceItem`. `pickaxe_head` 16종이 전부 레시피가 없어 **곡괭이를 못 만들던 것**이 풀렸다
- [x] **판 5종** — 압축기 `compress_{copper,gold,silver,bronze,invar}_plate` (주괴 1 → 판 1).
      `구리판` 하나가 컴퓨터 칩 → 마력 칩 → **코어 2티어**를 막고 있었다
- [x] **0티어 내리기** — `iron_plate` · `bucket` t1 → t0, 코어 `copper_plate` 신설.
      양동이 → 물 → 벽돌 → 화로·압축기가 0티어에서 이어진다
- [x] **강철·티타늄을 용광로로**(`blast_steel` 입력이 비어 있던 버그 포함) · **전기로 건설 레시피**
- [x] **화학 처리기 건설 레시피** (철근 콘크리트 + 드라이버 + 마력 파편2 + 철판2 + 전도체 가루4)
- [x] **물·용암을 초급 재단 전용으로**(코어 중복 삭제) · **시멘트 공장·유리 가공기 삭제**(레시피 4개 코어로)

도달 범위(폐쇄 시뮬레이션) — 코어 t0 **아이템 97 · 기계 15** / t1 **138 · 25** / t2 **159 · 35**

- [ ] ⚠ **지하맵이 철 주괴를 안 주면 0티어가 다시 막힌다** — 양동이가 철판 ×3 이고 철판은 철 주괴에서만 나온다.
      0-0 추출의 `철 조각 10%` → 화로 재련이 대안인데 그 화로를 지으려면 철판이 필요하다(순환)
- [ ] ⚠ **철근 콘크리트가 사실상 불가능한 양** — 철근 4 + 시멘트 **20**, 시멘트 1당 석회 1, 석회 1당 뼈 가루 2,
      뼈 가루는 0-0 추출 **1%** → 철근 콘크리트 1개에 돌 가루 4,000개.
      **2026-08-12 에 콘크리트를 2티어 재료로 올려 1티어는 풀렸지만**(§K), 이제 그 값이 **2티어 관문**이라
      2티어 기계 하나에 콘크리트 ×2 = 돌 가루 **약 8,000** 이다. 지하맵이 뼈 가루를 주거나 수치를 내려야 한다
- [ ] `1·2계열 추출기 8종`(마력석·운석)은 **2티어까지 안 쓰므로 논외**로 미뤘다 — 건설 레시피가 없다

### F-5. 개명 · 삭제 표시 — **에셋 미반영**

- [ ] `원유 채굴기`(1티어) → **`펌프`** ⚠ 에셋에 `Machine:Pump`·`Machine:OilDrill` 이 둘 다 있다
- [ ] 핵발전소 1티어 → **3티어** (철근 콘크리트 + 납 주괴 ×50 + 강화합금 ×200)
- [ ] Notion 이 레시피 칸에 `삭제` 라고 적은 3종 — `화약 제조기` · `유리 가공기` · `시멘트 공장` ·
      `아이템 강화기`. ⚠ **유리 가공기·시멘트 공장은 MachineBlock 도 레시피도 실재한다**
      (`glass_pipe` `glass_container` `acid_pipe` / `cement`) — 지우면 레시피도 함께

### F-6. 표기 · 수치 — **에셋 미반영**

- [ ] 합금 재련기가 **"광석·주괴 상관 없음" 을 버리고 주괴만** 받는다.
      산출도 `청동 주괴`·`인바 주괴` — 현재 에셋 표시명은 `청동`·`인바`·`강철`(주괴 없음)
- [ ] 화로 건설에서 **갈탄 ×1 제거** · 압축기 → 벽돌 + 드라이버 + 철판×8 + 전도체 가루×4 ·
      변압기 → 철근 + 금판×4 + 철판×4 + 구리판×4 · 전기 분해기·화학 처리기 재료 구체화
- [ ] 정밀 세공기 → 레이저 가공기×1 + **철판 ×50** · 수경 재배기 → 흙×20 + 특수합금×50 ·
      업그레이드 모듈 속도 = 특수합금×200 / 효율 = 강화합금×200
- [ ] 2티어 도구류 내구도 `무한` → `미정`

### F-7. 다음에 물어볼 것

1. **Notion 우라늄 주괴 비고가 아직 `1티어 (전기로)`** — F-2 의 새 규칙과 모순이다
2. **`삭제` 4종을 에셋에서도 지울지**
3. **`석유`(petroleum)** — F-1 로 만들 길이 없어졌다. 지울지, 증류기 산출로 되살릴지

---

## G. 3티어 (임시 기획 · 2026-08-07)

**설계 정본은 Notion 임시 페이지에 있다** — `🧪 3티어 (임시) — 저장 네트워크 + 핵발전소`
(`기획 - 테크트리` DB). 확정 전이라 `📔 기획 백로그 (수정본)` 에는 아직 안 합쳤다.

3티어 = **AE2 의 ME 시스템 같은 저장 네트워크 + 핵발전소**. 정한 것 셋 —
채널 제한 없이 **전력이 곧 한계**(컨트롤러40 + 드라이브20×3 + 터미널5 = 105/s vs 화력 20/s) ·
**이번엔 저장만**(자동 조합은 4티어) · **사용후 핵연료봉을 두고 화학 처리기가 재처리**.

### G-1. 왜 싸게 붙는가 (조사로 확인한 재사용 지점)

| AE2 | 재사용할 것 |
|---|---|
| ME 케이블 | `PipeKind` 에 값 하나 → `PipeRouter.Connects`·`ConnectionMask`·오토타일·`PipeNetworkManager` 렌더가 공짜 |
| Import/Export Bus | **렌치 면 상태**(`Insert`/`Extract`) 그대로 → 새 기계도 새 UI도 없다 |
| 서브넷 분리 | 렌치 `Cut` |
| 셀 내용(NBT) | `StorageCellInstance : ItemInstance` → **세이브 버전 상승 불필요**(v4 부터 슬롯별 ItemInstance) |
| ME 터미널 | `IItemContainer`(`UI/Slot/IItemContainer.cs`) 가상 구현 → `ItemSlot` 드래그앤드롭 그대로.<br>**이제 `SlotCapacity(index, item)` 도 구현해야 한다**(저장소 작업으로 멤버가 하나 늘었다) — 대신 망 전체를 한 칸으로 보여 주는 것도 공짜다 |
| Import/Export Bus | **이미 깔렸다** — 저장 블록의 `Insert`/`Extract` 면 규칙(`PipeRouter.CanInsertInto`/`CanExtractFrom`)이 그대로 버스다 |
| 채널 예산 | 안 만든다. `PowerLinkMode` + `energyUseRate` |

`PlaceableRecord` 새 필드 0 · 신규 파일 4개(`StorageCellInstance` `StorageNetwork`
`NetworkContainer` `StorageTerminalUI`) · 기존 수정은 `PipeKind` 값 1개 + `MachineInstance` 훅 정도.

### G-2. 구현 전에 반드시 처리할 것 — **A축으로 전부 끝났다 (2026-08-17)**

- [x] `ChemicalProcessor` 출력 1 → **2** (재처리가 우라늄 농축물 + 납 주괴 둘을 낸다)
- [x] `PrecisionLathe` 입력 2 → **3** (핵연료봉이 재료 3종)
- [x] `NuclearPlant` 출력 0 → **1** (사용후 봉 자리) · UI 는 공유 `Generator_UI` 에 출력칸을 넣었다
- [x] **발전기 산출 훅** — `MachineBlock.spentFuelItem` + `MachineInstance.TryEjectSpentFuel`.
      `TickGenerator` 를 레시피 경로로 끌어오지 않고 **연소 경로에만** 걸었다.
      ⚠ 점화 직전에 **낼 몫이 없어도 자리를 미리 확인**해야 한다 — 안 그러면 출력이 꽉 찬 상태에서도
      연료를 하나 더 태우고 찌꺼기를 `burnTotal` 에 빚으로 떠안는다(실측으로 걸렸고 고쳤다).
      "아직 안 낸 찌꺼기" 표시는 `burnRemaining == 0 && burnTotal > 0` — 둘 다 v5 필드라 **세이브 v12 그대로**.
- [x] ~~3티어 신규 제작은 전부 코어 조합기~~ → **한 곳만 예외다.** 승급 재료 `공명 칩` 은 **고급 재단(t2)**
      에 둔다. 코어 3티어 목록에 두면 *3티어가 되어야 3티어로 올릴 재료를 만들 수 있어* 영원히 잠긴다.
      (§F-0 의 "고급 조합기 처리 티어 1" 경고는 §N-0 로 무효 — `AdvancedCrafter.tier` 는 2 다)

### G-3. 선행 조건 — **네 건 모두 해소 (2026-08-17 실측)**

- [x] **특수합금 · 강화 합금** 아이템 (§F-4) — 둘 다 존재하고 폐쇄 검증에서 도달 O
- [x] **코어 업그레이드 1·2차** (§A-5) — 구현돼 있었다. 3차(`공명 칩 → 3`)를 표에 한 줄 더했다
- [x] **정밀 세공기 · 화학 처리기 건설 레시피** (§C-5·C-6) — 둘 다 에셋에 입력까지 채워져 있었다
- [x] **우라늄 농축물 사슬** (§F-3) — 도달 O. 이제 소비처(`nuclear_fuel_rod`)도 생겼다

### G-2b. A축 결과 (2026-08-17)

새로 만든 것: 아이템 4종(`spent_nuclear_fuel_rod` `resonance_crystal` `resonance_chip` `storage_substrate`) ·
레시피 6개(`nuclear_fuel_rod` `chem_reprocess` `nuclear_plant` `resonance_crystal` `resonance_chip` `storage_substrate`).
`NuclearPlant` = tier 3 · `fuelBurnRate 500` · `maxEnergyAmount 20000` · 봉 `burnEnergy 30000`(정확히 60초).

⚠ **`conductor_crystal` 하나가 3티어 전체를 막고 있다** — 유일한 획득처가 `extract_shiny_powder`
(**1-0티어 추출기**, 5%)인데 1계열 추출기 4종에 건설 레시피가 없다(사용자가 따로 정하기로 한 범위).
분쇄 사슬은 `manastone_fine_dust` 까지 전부 도달하므로 **`Machine:Extractor10` 건설 레시피 하나면**
공명 결정 → 공명 칩 → 코어 3차 → 3티어 전부가 함께 열린다. **핵발전소도 그때까지는 못 짓는다**
(연료봉 제작·재처리 사슬 자체는 t2 에서도 돈다).

### G-5. B축 결과 — 저장 네트워크 (2026-08-17 · 구현 완료)

신규 파일 6개 — `StorageNetworkRole` `StorageNetworkBlock` `StorageNetwork` `StorageCellItem`
`StorageCellInstance` `NetworkContainer` `StorageTerminalUI`(+ `DefaultMachineUI.RebindInputs`).
에셋: 블록 3 · 아이템 12(장치3 + 부품4 + 셀4 + 케이블1) · 레시피 12 · `StorageTerminal_UI` 프리팹.
**세이브 v12 그대로** — 구성은 파생 상태고 셀 내용만 `ItemInstance` 로 붙는다.

실측(플레이 모드): 컨트롤러+케이블2+드라이브(4k 셀)+상자2, 케이블 북=Extract·남=Insert →
`상자A 300 → 망 → 상자B` 가 초당 9~10개로 흐르고, 전력을 끊으면 멈추고 복구하면 다시 흐른다.

**구현하며 잡은 함정 셋(전부 실측으로 걸렸다)**

- ⚠ **전력을 Build 시점에 캐시하면 안 된다.** 위상은 렌치·케이블에만 반응하는데 전원은 매 프레임 바뀐다 —
  컨트롤러가 먼저 켜지며 망을 캐시해 드라이브가 영영 `0/1` 이었다. `Status`·`Cells()` 가 매번 다시 본다.
- ⚠ **버스 예산을 공유하면 뒤쪽 버스가 굶는다.** Extract 가 다 써서 Insert 가 한 개도 못 옮겼다 —
  버스마다 `BusRate`(8/초)를 따로 준다.
- ⚠ **발전기 산출과 같은 계열의 함정** — 못 넣은 몫은 되돌려야 한다(`Fill` 이 `LogError` 로 드러낸다).

**남은 것**: 전용 아트가 없어 장치 3종이 `ItemStorage` 프리팹을, 데이터 케이블이 아이템 파이프 밴드를
빌려 쓴다. 아이콘 12개가 `assetPlaceHolder` 다.

### G-4. 확정되면 할 것

- [ ] Notion 임시 페이지를 `📔 기획 백로그 (수정본)` 에 합친다
      (3티어 표 신설 · 핵발전소를 2티어 표에서 옮김 · 코어 업그레이드 3차 행 · 자원 목록에 새 아이템 10종)
- [ ] `레시피.md` 에 `# 3티어` 절 신설

---

## H. 2026-08-08 — 3티어 착수 전 5건 구현 완료

`C:\Users\c\.claude\plans\1-4-partitioned-starlight.md` 의 9단계를 전부 실행했다.
**아래는 §A~§G 중 이번에 해결된 것과, 새로 생긴 숙제다.**

### H-1. 끝난 것

- [x] **곡괭이 채굴** — `ToolDefinition.canMineBlocks`(곡괭이만) · 벽 한 칸당 내구도 1 · 0 이면 스택 소멸.
      기계·파이프는 맨손으로도 캔다(곡괭이가 부러졌을 때 공장을 못 뜯어 갇히지 않게).
- [x] **마법 재단 3종을 조합대로** — 초급(0)·중급(1)·고급(2, 신설)이 `CraftingTableBlock` +
      `recipeGroupId = "Altar"`. 재단 레시피 11개가 한 목록으로 합쳐졌다.
      `special_alloy`(특수합금) 아이템 + 합금 재련기 레시피 신설. `AdvancedCrafter.tier` 1 → **2**
      (자기 전용 레시피 `advanced_altar` 를 띄우려면 필요했다 — 전용 목록이 0개 → 1개).
      `magic_water`·`magic_lava` 가 산출물 없이 죽어 있던 것도 살렸다.
- [x] **유체 시스템** — `FluidDefine`(액체·기체 통합) · `FluidStack` · `Recipe.fluidInputs/fluidOutputs` ·
      `RecipeSolver` 유체 API 7개 · 기계 탱크 · 양동이 교환 · 파이프 운반 · 세이브 **v11**.
      유체 에셋 8종, 탱크를 가진 기계 5종, 유체 레시피 8개. `electrolyzer_water`(빈 껍데기 중복) 삭제.
- [x] **업그레이드 모듈** — `UpgradeModuleItem`(수치는 에셋 필드) · 일반 기계 2칸 · 세이브 **v12**.
      속도 +25%/개, 효율 −10%/개(전력·연료 둘 다). 발전기는 반대로 산출 쪽에 건다.
- [x] **코어 업그레이드 1·2차** (§A-5 해결) — `CoreUpgradeTable`(static 정본) ·
      `PlaceableRecord.tier` · 코어 조합기 UI 의 칸+버튼. 레시피 30 → 54 → 65 개로 열린다.
- [x] **UI 재료** (§요청 3) — `MachineUIRole.UpgradeSlot=10` · `UpgradeSlot.prefab` ·
      팩토리 자동 배치(작동 버튼 누락도 함께 수정) · 검증기 규칙 · 유체 바 이름 정리.

### H-2. 이번에 새로 생긴 숙제

- [ ] **`chem_acid` 는 산성 용액을 출력 탱크에 내는데 `chem_uranium` 은 입력 탱크에서 먹는다.**
      같은 기계 안에서 안 이어진다 — 파이프로 되돌릴지, 유리 용기로 퍼 나를지 정해야 한다.
- [ ] **마나 스케일** — `마나 100 = 마력결정 1`(레시피.md 정본)이라 물(1000/양동이)과 자릿수가 다르다.
      mL 로 옮길 때 함께 정한다.
- [ ] **고급 재단 전용 레시피 0개** — 중급과 같은 11개만 보인다. 2티어 마법 레시피를 정해야 한다.
- [ ] **증류기 `원유 → 가스`** — 레시피도 가스 아이템도 없어 탱크를 주지 않았다.
- [ ] **핵발전소 탱크 + `TickGenerator` 산출 훅** — §G-2 와 같은 항목. 3티어에서 함께.
- [ ] **`강화 합금`** 아이템 (§F-4 의 절반) — 특수합금만 만들었다.
- [ ] 기계 UI 프리팹에 업그레이드 칸·유체 바 배치 — **사용자가 직접 만든다**(요청 3).
      지금은 요소가 없어 런타임에 조용히 클램프된다.

### H-3. §A~§G 중 아직 남은 것

§A-5(코어 업그레이드)는 H-1 로 해결. §F-4 는 절반(특수합금만). 나머지는 그대로다.
§G-2 의 슬롯 수 3건과 산출 훅, §G-3 의 선행 조건 4건은 3티어 착수 시점에 처리한다.

---

## I. 2026-08-10 — 지하맵 (완료)

탐지기로 여는 **저장되지 않는 7×7 인스턴스 방**. 자세한 규약은 `CLAUDE.md` §4 "지하맵" 과 "씬 구성".

### I-1. 한 일

- [x] **공용 rig 를 `Assets/Prefabs/Core/GameRig.prefab` 으로 추출** — MapTest 의 비-persist 루트 14개를
      한 프리팹에 담아 지상·지하 두 씬이 공유한다. persist 싱글톤 8개는 씬 루트로 남겼다
      (`DontDestroyOnLoad` 가 루트에만 듣기 때문).
- [x] **`WorldMap` 월드 교체** — `chunkGenerator` 델리게이트 + `IsEphemeral` +
      `EnterEphemeralWorld`/`ReturnToPersistentWorld`. **세이브 포맷 변경 0(v12 그대로)**.
      쓰이지 않던 `EnsurePrototypeUndergroundRoom()` 삭제.
- [x] **지형 3종** — `wall:meteorite`(운석) · `wall:bedrock`(암반, dropItem 없음 = 채굴 불가) ·
      `floor:water`(물). 물 스프라이트·타일도 새로 만들었다(PPU 32 · Point · 무압축).
- [x] **`UndergroundPalette`**(등급→벽·바닥, 반지름, 탐지기→등급, 발견 확률) ·
      **`UndergroundLootTable`**(보상 6행) · **`UndergroundWorld`**(방 생성) · **`UndergroundSession`**(상태).
- [x] **`PrototypeMapTransitions` → `UndergroundPortal`** 로 갈아엎고 `UndergroundSceneSetup` 을 정리.
      `UndergroundScene` 은 `GameRig` + 셋업 둘뿐이다.
- [x] **`PlayerSave` 지하 가드 2개** · **다우징 로드 우클릭 10%** · 콘솔 `/underground <등급>`.

### I-2. 이번에 고친 잠복 버그

- **`Singleton.Awake` 가 `_instance == this` 를 놓쳤다** — `Awake` 보다 먼저 `Instance` 가 불리면
  `DontDestroyOnLoad` 가 영영 안 걸린다. `InputActionManager` 가 실제로 씬과 함께 죽고 있었다.
- **`TooltipUI` 가 씬을 넘어 살아남고 있었다** — 패널은 캔버스와 함께 죽으므로 씬 전환 뒤
  툴팁이 영영 안 뜬다. `PersistAcrossScenes => false` 로 바꿨다.
- **`PlayerForTest` 가 플레이어에 `DontDestroyOnLoad`** 를 걸고 있었다(프로토타입 잔재).
  두 씬이 같은 rig 를 쓰므로 플레이어가 둘이 된다 — 제거.
- **지하에서 주운 것이 지상에 못 나왔다** — 복귀 시 `PlayerSave.Load` 가 *내려가기 직전에 쓴*
  `player.dat` 로 인벤토리를 되돌린다. `UndergroundSession.Exit` 이 **`IsActive` 를 내리기 전에**
  `PlayerSave.Save()` 를 부르도록 고쳤다(그 순간이라 좌표는 지상 자리로, 인벤토리는 지금 것으로 쓰인다).

### I-3. 남은 것

- [ ] **아트가 임시다** — 암반이 돌과, 운석이 마력석과 똑같이 보인다(아틀라스 재활용). 물은 단색 파랑.
- [x] **물을 퍼낼 수 있다**(2026-08-11) — 빈 양동이로 `floor:water` 우클릭 → `양동이(물)`.
      바닥은 줄지 않는 **무한 원천**이고, 이것이 **물의 유일한 최초 획득처**다.
      정본은 `MainBlock.fluid` 한 필드 + `PlayerInteraction.TryFillContainer` 한 분기.
      ⚠ **여전히 통행은 막지 않는다** — 그래서 이 분기만 발밑(자기가 선 칸)도 허용한다.
- [ ] **붓기가 없다** — 채워진 그릇을 땅에 부어 유체 타일을 놓는 것. `WorldMap.PlaceFloor` 가 이미 있어
      붙이기는 쉽지만 **"놓은 물을 다시 퍼면 무한 증식"** 이라 소모/유한 원천 규칙을 먼저 정해야 한다.
- [ ] **유리 용기 오버레이 아트가 없다** — `산성 용액` 만 합성 아이콘이 아니라 플레이스홀더 한 장이다.
      `glass_container.fluidOverlay` 에 흰색 마스크를 넣으면 코드 변경 없이 붙는다.
- [ ] **포탈이 저장되지 않는다** — 찾으면 그 자리에서 들어가야 한다. 영속 포탈을 만들려면
      `blockId == itemName == blockName` 규약을 따르는 배치물 3종(블록·아이템·프리팹)이 필요하다.
- [ ] **탐지기가 t0 하나뿐이다** — `UndergroundPalette.DowsingTierOf` 에 `dowsing_rod_t1/t2` 자리는
      이미 있다. 아이템과 레시피만 만들면 1·2등급 방이 열린다.
- [ ] **`dowsing_rod_t0` 가 그림까지 같은 중복**이다 — `ItemAliases` 로 `dowsing_rod` 에 흡수할 것.
- [ ] **전리품 표는 초안이다**(`UndergroundLootTable.Rows`). 실측: t0 한 판에 20~25칸.
      ⚠ `iron_ingot`·`mana_shard` 행을 빼면 0티어가 다시 막힌다.
- [ ] **지하에도 기계를 지을 수 있다** — 나가면 방과 함께 사라지므로 잃어버리기 쉽다. 막을지는 미정.
- [ ] **UndergroundScene 을 직접 Play 하면 안 된다** — 딕셔너리·인벤토리가 MapTest 에만 있다.

---

## J. 2026-08-10 — 압연기를 압축기로 통합 (완료)

`벽돌 공장 → 압연기 → 압축기` 로 **두 번 통합**됐다. "같은 기계가 두 티어에 있으면 안 된다" 규칙
(CLAUDE.md §4)의 위반이었고, 압연기 쪽 값(입력 2칸 · UI)이 더 완성돼 있어 **에셋은 압축기를 남기고
값만 이식**했다.

- [x] `Machine:Compressor` 에 압연기 값 이식 — `inputSlotCount` 1 → **2**(`compress_brick` 이 모래2+물1),
      `uiPrefab` 은 옛 `RollingMill_UI`(입력2 + 업그레이드2)를 **`Compressor_UI` 로 개명**해 물려받았다.
- [x] 가공 레시피 **8개를 `Machine:Compressor` 로** 옮기고 `compress_*` 로 개명
      (`rolling_*_plate` 5 · `rolling_iron_plate` · `roll_silicon_plate` · `brick_factory_brick`).
      지금 압축기 목록은 **11개**이고 재료가 서로 달라 `SelectRecipe` 함정에 안 걸린다.
- [x] 에셋 4개 삭제(`roller` 레시피 · `RollingMill` 블록·프리팹·아이템) ·
      `MachineBlockFiller.NewMachineIds` 에서 압연기 줄 제거(안 지우면 다음 실행에 되살아난다).
      ⚠ 같은 자리에 남아 있던 `시멘트 공장`·`유리 가공기` 줄도 함께 지웠다 — §F 에서 지운 기계들이다.
- [x] 옛 이름 잇기 — `ItemAliases`(`Machine:RollingMill` → `Machine:Compressor`, `압연기`·`압축기`) ·
      `MachineAliases`(`압연기`·`벽돌 공장` → `압축기`). **`ItemAliases.Resolve` 는 한 단계만 푼다**
      — 그래서 `벽돌 공장` 도 중간 이름이 아니라 **최종 이름을 직접** 가리킨다.
- [x] `StreamingAssets/Recipes/*.json` 의 `id`·`machine`·`note` 4파일 갱신.
      **안 맞추면 `Import JSON Recipes` 가 옛 이름으로 통째로 되살린다.**

### J-1. 남은 것 / 짚어 둘 점

- ~~⚠ 압축기 건설 레시피가 t1 이고 철근 콘크리트를 요구해 압축기 레시피 11개가 통째로 막혀 있었다~~
  ✅ **2026-08-12 해소** — 1티어 기본 재료가 `철근 ×4`(= 철판 8)로 내려갔다(§K).
- 다행히 **0티어 부트스트랩은 안 막힌다** — `철판`·`구리판`은 코어 조합기 t0 경로가 따로 있다.
- ⚠ 사슬 레시피 **파일 이름은 아직 옛 아이템 이름 그대로**다(§B-7) — 이번 개명 범위 밖이었다.

---

## K. 2026-08-12 — 건설 재료 티어 규칙 + 2티어 재료 채우기 (완료)

**정한 것** — 기계 건설 레시피 = **티어 기본 재료 + 기계별 부품**.

| 해금 티어 | 기본 재료 | 실질 비용 |
|---|---|---|
| 0 | `벽돌 ×1` | 모래 2 + 물 1 |
| 1 | **`철근 ×4`** (이전: 철근 콘크리트 ×1) | 철판 8 |
| 2 | **`철근 콘크리트 ×2`** (이전: 없음) | 철근 8 + 시멘트 40 |

- [x] **1티어 8개**를 `철근 ×4` 로 — `alloy_smelter` `bio_incubator` `chemical_processor` `compressor`
      `electrolyzer` `laser_processor` `mana_dissolver` + `auto_crafter`(기계 에셋은 없지만 t1 건설 레시피다).
      **콘크리트를 재료로 쓰는 레시피는 이제 0개다.**
- [x] **새 아이템 2종 + 레시피 2개** — `reinforced_alloy`(강화 합금) · `machine_upgrade_module`(기계 강화 모듈).
      `Machine:PrecisionLathe.inputSlotCount` 1 → 2 와 `PrecisionLathe_UI` 입력 칸 추가가 함께 필요했다.
      `ItemAliases` 에 `강화 합금` · `기계 강화 모듈` · 옛 이름 `2티어 업그레이드 모듈` 세 줄.
- [x] **2티어 기계 10개**의 `inputs` 를 노션 표 값으로 채우고 전부에 `철근 콘크리트 ×2` 를 붙였다(§B-4 해소).
- [x] **JSON 동기화** — `crafting.json`(14) · `notion_machines.json`(16) · `notion_magic.json`(1) +
      `machine_processing.json` 에 새 레시피 2개. 임포터는 기존 에셋을 건너뛰지만, **에셋을 지웠다 다시
      임포트하면 옛 재료로 살아나므로** 맞춰 둔다.
- [x] **노션 `기획 백로그 (수정본)` 을 08-07 이후로 전부 동기화** — 08-12 변경 요약 문단 신설 ·
      §3 자원 목록(압연기 → 압축기, 양동이 0티어, 강화 합금·기계 강화 모듈 행 추가) · §5 인프라 규칙 ·
      0티어 표에서 압연기 행 삭제 · 1·2티어 표 재료 · §6 가공 레시피. `3티어 (임시)` 페이지의
      "티어 기본 재료 = 철근 콘크리트" 도 갱신했다.

### K-1. 이번에 생긴 부채

- [ ] ⚠ **2티어가 콘크리트 값을 그대로 물려받았다** — 콘크리트 1개 ≈ 돌 가루 4,000 이므로
      **2티어 기계 하나에 약 8,000**. §C 의 "콘크리트 수량을 고쳐야 한다" 가 이제 2티어 관문 문제가 됐다.
      뼈 가루(0-0 추출 1%)를 지하맵이 주거나 시멘트 수량을 내리는 것이 후보다.
- [ ] ⚠ **`item_enhancer`(아이템 강화기)는 기본 재료 ×2 뿐**이다 — 공장별 부품이 노션에도 `레시피.md` 에도
      없어서 채우지 못했다. **2티어 기계 중 유일하게 콘크리트 2개로 만들어진다.**
- [ ] ⚠ **정밀 세공기의 `모터`·`베어링`·`프로펠러` 3레시피는 여전히 입력이 비어 있다.**
      노션은 셋 다 `강화 합금 ×10` 인데 **그대로 넣으면 재료가 완전히 같아져 `SelectRecipe` 가
      첫 번째만 영원히 돌린다.** 재료를 서로 다르게 정하는 것이 먼저다.
- [ ] ⚠ **`펌프` 의 티어가 문서와 에셋에서 다르다** — 노션은 1티어, 에셋은 t2. 이번엔 에셋을 정본으로 보고
      부품만 노션에서 가져왔다(철판 20 + 전도체 가루 10 + 콘크리트 2). 어느 쪽으로 할지 결정 필요.
      노션의 2티어 `원유 펌프` 는 별개 기계 `Machine:OilDrill` 인데 **건설 레시피가 없다.**
- [ ] `reinforced_concrete` 레시피의 `tier` 가 **0** 이다(재료인 시멘트는 t1). 이제 2티어 관문 재료이므로
      해금 티어를 올리는 편이 자연스럽다 — 지금은 t0 목록에 뜨지만 시멘트가 없어 못 만든다.

---

## L. 여러 칸을 차지하는 기계 (발자국 + 설치 미리보기) — 2026-08-13

### L-1. 한 일

- [x] **`MachineBlock.size` + `Footprint`** — 발자국은 **저장하지 않는다**(SO 파생, 세이브 v12 그대로).
      `Mathf.Max(1, …)` 클램프가 "새 필드는 0 으로 읽힌다" 함정을 대신 막는다.
- [x] **`WorldMap` 에 점유 색인** — 덮인 칸 → 기준점. `GetPlaceableAt` 이 이걸로 해석하므로
      `PipeRouter` 의 `MachineAt·Connects·StorageAt·FaceAt` 와 배치 판정이 **코드 변경 없이** 발자국을 안다.
      쓰기도 `SetPlaceableAt`/`RemovePlaceableAt`(월드 좌표)로 모아 좌표계 비대칭을 없앴다.
- [x] **`MapGenerator` 가 덮는 칸마다 같은 인스턴스를 등록** — 사용자가 겪은
      "나머지 칸은 우클릭해도 UI 가 안 열린다" 가 여기서 풀린다. 위치는 발자국 정중앙으로 보정하고,
      콜라이더는 **인스턴스에만** 발자국 크기로 맞춘다(`tmp_crafter` 는 콜라이더가 없어 새로 붙인다).
- [x] **`PlacementPreview`** — 반투명 기계 그림(order 8) + 칸별 초록/빨강(order 7).
      판정은 `PlayerInteraction.CanPlaceFootprint` **한 곳**만 하고 미리보기는 결과만 칠한다.
- [x] **기준점 정규화 4곳** — `PipeRouter.AddSink` · `PipeNetworkManager` 의 `sourceCell` ·
      `PowerLinkMode.AddLink` · `MapGenerator.FlushAll`.
- [x] 에셋 6종: 고급 조합기 5×5 · 고급 재단 3×3 · 코어 조합기 3×3 · 중급 재단 2×2 · 용광로 2×2 · 증류기 1×3.
- [x] ⚠ **정렬 순서 마이그레이션 누락 2건을 함께 고쳤다.** 정렬 순서가 `(옛값 + 10) × 10` 으로 바뀔 때
      `PipeFaceOverlay`(5) 와 `PowerLinkMode`(6) 가 빠져 있었다 — 둘 다 바닥(100)보다 아래라
      **렌치 면 막대와 전송 모드 오버레이가 통째로 안 보이던 상태**였다. 150·160 으로 올렸다.
      코드에 남은 한 자릿수 값은 이제 0건이다.

### L-2. 이번에 생긴 부채 · 손대지 않은 것

- [ ] **회전이 없다.** 발자국은 축 정렬 직사각형이라 `1×3` 증류기는 세로로만 놓인다.
- [ ] ⚠ **5×5 고급 조합기는 빈 땅 25칸을 요구한다.** 실제로 놓아 보고 답답하면 그림을 줄이거나 3×3 으로 낮춘다.
- [ ] ⚠ **면 상태는 방향당 하나다** — 2×2 의 북쪽 두 칸이 렌치 설정 하나를 공유한다.
      칸별로 나누려면 `faceModes` 를 레코드가 아니라 칸에 둬야 해서 세이브가 바뀐다.
- [ ] **발자국이 청크 경계를 넘고 기준점 청크만 언로드되면** 덮인 칸에 인스턴스가 없다.
      레코드는 점유 색인으로 계속 보이고 배달은 `IsCellLoaded` 가 막으므로 안전하지만,
      렌더 창(25청크) 가장자리에서만 생기는 상태다.
- [ ] **`Furnace.prefab` 의 월드 스프라이트가 `furnace_icon`(22×30)** 로 잘못 물려 있고,
      `HVElectricFurnace.prefab` 은 `machine2.png` 의 16×25 화로를 쓴다. 그림을 고치면 발자국도 다시 재야 한다.
- [ ] **아이콘 3종이 월드 스프라이트를 그대로 쓴다** — `AdvancedAltar`(86×99) · `IntermediateAltar`(56×67) ·
      `CoreCrafter`(82×82). 앞의 둘은 32×32 `_icon` 스프라이트가 이미 있는데 안 쓰여 슬롯에서 넘친다.
- [ ] ⚠ **`Chunk.Save/Load` 가 `plantedAtUtcTicks` 를 두 번 쓰고 두 번 읽는다**(`WorldMap.cs:232-233` /
      `404-406`). 자기끼리는 맞아 돌지만 gate 가 `version >= 9` 라 **진짜 v9~v11 파일은 못 읽는다.**
- [ ] **`MachinePlacement.cs`(21-42)는 죽은 병행 배치 코드**다. 한 칸 가정을 복사해 갖고 있으니 지우는 편이 맞다.
- [ ] **`PlayerInteraction`** 에 죽은 가지 하나(`:474-475` `MineTarget.Crop` 중복)가 남아 있다.
      (`IsFloor` 블록 중복은 이번에 판정을 합치면서 사라졌다.)
- [ ] **덜 자란 작물을 없앨 방법이 없다** — `HarvestCropAt`·`MineTarget.Crop` 둘 다 `IsMature` 를 요구하고,
      레코드가 남아 다시 놓지도 못한다.

---

## M. 맵 조명 (전역 어둠 · 벽 그림자 · 횃불 · 전등) — 2026-08-14

### M-1. 한 일

- [x] **전역 어둠** — `LightingPalette`(정본 표) + `MapLighting`(런타임 생성). 메인 맵 `#3A4A6B · 0.18`,
      지하 인스턴스 방 `0.12`. **메인 맵도 설정상 지하**라 낮/밤은 만들지 않았다.
- [x] **플레이어 상시 소광** — inner 0.6 / outer 3.5 / intensity 0.55, 그림자 켬.
- [x] **월드 UI 오버레이 4종을 조명에서 제외**(`OverlayMaterial` 공유 Unlit) —
      커서 윤곽선 · 파이프 면 막대 · PowerLink 오버레이 · 설치 미리보기.
- [x] **`LightBlock : MachineBlock`** + `MachineBlock.IsAlwaysOn`/`OpensUI` +
      `MachineInstance` 상시 전력 소비 분기 + `IsRunning` + `LightEmitter`.
- [x] **횃불**(t0, 석탄1+돌1 → 2개, 무전력, flicker) · **전등**(t1, 철판2+유리1+전도체가루1 → 2개,
      2/s 상시 소비, 전력 없으면 꺼짐) 각각 블록 SO + 프리팹 + 아이템 + 레시피.
- [x] **벽 그림자** `ChunkShadowCasters` — 그리디 메싱 + 풀 + `CompositeShadowCaster2D`.
- [x] `Renderer2D.m_LightRenderTextureScale` 0.5 → 1(반해상도라 빛 경계가 뭉갰다).

### M-2. 이번에 생긴 부채

- [ ] ⚠ **아트가 전부 플레이스홀더다.** 횃불·전등의 월드 스프라이트(~30×30)와 아이콘(~26×22)이 필요하다.
      지금은 `assetPlaceHolder`("No Asset")가 그대로 보인다.
- [ ] ⚠ **전등 재료가 티어 기본 재료 표를 따르지 않는다.** 1티어 기본은 `철근 ×4`(= 철판 8)인데
      조명은 수십 개를 까는 물건이라 `철판 2 + 유리 1 + 전도체 가루 1` 로 낮춰 잡았다.
      표는 *기계* 건설 규칙이고 해금을 잠그지도 않지만, **의도적인 예외라는 것을 기록해 둔다.**
- [ ] **횃불이 `석탄 + 돌` 이다.** 원래는 나무 막대가 맞지만 **`stick`·`wood` 아이템이 없고**
      `wood_rod` 는 나무 재질에 재료가 없어 만들 수 없다(§CLAUDE.md 도구 항목). 나무가 생기면 바꾼다.
- [ ] **횃불은 영구히 탄다** — 연료를 소모하지 않는다. 수명을 주려면 `CropInstance` 처럼
      `plantedAtUtcTicks` 기반 UTC 타이머가 가장 싸다(세이브 변경 0).
- [ ] **전등에 on/off 스위치가 없다** — 전력이 있으면 켜진다. 렌치로 끄는 것은 나중에.
- [x] ⚠ **벽 그림자가 실제로는 하나도 안 그려지고 있었다**(2026-08-14 사용자 지적으로 발견).
      캐스터는 29개 다 서 있었지만 `ShadowCaster2D` 가 `SpriteRenderer` 를 보고 sprite provider 를 골라
      **빈 그림자 메시(0 verts)** 를 만들고 있었다. 캐스터에서 `SpriteRenderer` 를 떼어 `ShapeEditor`
      경로로 돌리니 20 verts · boundingSphere 11.31 로 정상. **개수만 세고 메시를 안 본 것이 원인**이라
      검증 항목을 `mesh.vertexCount > 0` 으로 바꿨다.
- [ ] **큰 벽 사각형은 자기 자신을 가리지 않는다**(`selfShadows = false`). 그래서 횃불 하나가
      맞닿은 암반 면을 통째로 밝힌다. 더 좁게 보이게 하려면 사각형을 잘게 쪼개거나
      `selfShadows` 를 켜야 하는데, 켜면 벽이 통째로 검어진다 — 실제로 놀아 보고 정한다.
- [ ] **`Renderer2D.m_MaxShadowRenderTextureCount` 는 1 그대로다.** 그림자를 내는 조명이 여럿 겹칠 때
      모자라는지 아직 확인하지 못했다(검증에서는 조명 3개까지만 켜 봤다).
- [ ] **`ShadowCaster2D.Update()` 가 캐스터마다 매 프레임 돈다.** 지금은 29개라 무시할 수 있지만
      플레이어가 넓게 파헤치면 늘어난다 — 청크당 사각형 수를 지켜볼 것.
- [ ] **지하 씬의 환경광 전환은 코드로만 확인했다.** 실제로 포탈을 타고 내려가 보지는 않았다.

### M-3. 런타임 생성 → 에디터 저작 이관 (2026-08-14, 사용자 지침)

**규칙: 오브젝트는 에디터(프리팹) 저작이 기본이고, 개수·모양이 데이터에 달린 것만 런타임.**
CLAUDE.md 의 "런타임 UI 구성이 규약" 을 뒤집었다(§UI). 두 씬이 `GameRig` 한 장을 쓰고
PrefabInstance override 가 transform·이름뿐이라 **프리팹만 고치면 두 씬에 전파된다** — 옛 명분이 성립 안 했다.

- [x] `MapLighting` → `GameRig/Global Light 2D` 에 컴포넌트로 저작(`globalLight`·`playerLight` 직렬화).
- [x] `PlayerLight` → `GameRig/TestPlayer` 자식으로 저작(값은 여전히 `LightingPalette` 가 채운다).
- [x] `ChunkShadowCasters` → `GameRig/Map` 자식으로 저작(`reference`·`whitePixel` 직렬화).
      청크별 사각형은 개수가 월드에 달려 있어 **그대로 런타임**.
- [x] 공용 에셋 3개 신설(`Assets/Asset/Common/`) + `OverlayMaterial.cs` 삭제 · `Shader.Find` 제거 ·
      네 곳의 1×1 스프라이트 생성 코드 제거.

**남은 이관 대상** (이번엔 잘 돌고 있어 손대지 않았다):

- [ ] **코드로 짓는 UI 5종** — `CommandConsole` · `ItemBrowser` · `TooltipUI` · `PowerLinkMode` 패널 ·
      `DefaultMachineUI.BuildPowerLinkButton`(CLAUDE.md 가 이미 안티패턴이라 적어 둔 마지막 ① 잔재).
      `CraftingTableUIFactory` 가 프리팹을 굽고 `CraftingTableUI` 가 템플릿을 복제하는 방식이 본보기다.
      ⚠ `TooltipUI` 가 `PersistAcrossScenes => false` 를 강제당하는 것도 코드로 지어서 생긴 문제다.
- [ ] **런타임 타일맵 2종** — `PipeNetworkManager` 의 `Pipes`, `PowerLinkMode` 의 `PowerLinkOverlay`.
      정렬 순서 상수(120·160)가 코드에 박혀 씬의 정렬 체계와 두 곳으로 갈려 있다.
      ⚠ `PipeNetwork` 호스트는 **부모가 없어 씬 루트에 뜬다**(`SetParent` 호출 자체가 없다).
- [ ] `PlacementPreview` 루트와 `Ghost`, `MapGenerator` 의 `Placeables`/`Drops` 빈 컨테이너.
- [ ] **런타임 `Tile` 생성** — `PipeNetworkManager.TileFor`(블록당 16개)가 가장 크다.
      `PipeAtlasBuilder` 가 이미 있으니 구울 수 있다. 런타임 `ScriptableObject` 는 파괴되지 않아
      도메인 리로드 때 leak 경고가 뜬다. `TilemapTextureLoader` 의 `floorTile`·`CreateRuntimeTile` 도 같은 성격.
- [ ] **프리팹에 빠진 컴포넌트를 코드가 때우는 자리 둘** — `MapGenerator` 의
      `AddComponent<MachineInstance>` · `AddComponent<BoxCollider2D>`("tmp_crafter 처럼 아예 없는 프리팹도 있다").
- [ ] **작물** — `SpawnCrop` 이 맨 GameObject 를 짓는다. `Crop.prefab` 한 장이면 `Instantiate` 로 접힌다.
- [ ] `InputActionManager` 는 액션맵을 코드로 만든다 — `.inputactions` 에셋으로 뺄 수 있다.

---

## N. 2026-08-15 — 2티어까지 막힌 것 뚫기 (완료)

**사용자가 정한 것** — 범위는 "막힌 것 + 못 짓는 기계"(2티어 기계 4종의 *가공* 레시피 신설은 범위 밖) ·
1계열 추출기와 탐지기 t1 은 따로 확정 · 콘크리트는 뼈 가루 획득처 추가로 · 무한 생산 5개는 재료를 서로 다르게.

### N-0. ⚠ 티어는 한 축뿐이다 (2026-08-15 확정 · 규칙 변경)

> **"기계는 모든 티어의 재료를 가공한다. 분쇄기도 화로도 한 종류가 전부를 처리한다.
>  `tier` 는 코어 조합대 목록에 언제 나타나는지에만 쓴다."**

`§F-0`("기계에는 티어가 없다")의 논리적 귀결이다. 코드에서 **처리 게이트 세 곳**을 지웠다:

- [x] `MachineInstance.SelectRecipe` — `recipe.tier > Tier` 삭제
- [x] `PipeRouter.TargetSlots` · `TargetTanks` — 같은 비교 삭제.
      **⚠ 이 둘을 놓쳤으면** "기계는 돌릴 수 있는데 파이프가 재료를 안 넣는" 상태가 됐다(자동화에서만 드러난다)
- [x] `MachineBlock.tier` 툴팁·`MachineInstance.Tier` 주석을 정반대로 고침(지금은 조합대 전용이라고 적혀 있다)

**실측: 도달 범위가 하나도 안 늘었다**(지하 열림 t2 기준 135/35 그대로). 콘텐츠를 푸는 변경이 아니라
**없는 문제를 만들던 개념을 지운 것**이다. 대신 죽어 있던 레시피 13건이 살아났다 —
화로 +4(`smelt_lead/nickel/tin/uranium`) · 수동 분쇄기 +6 · 전기 분쇄기 +3(운석 사슬).

⚠ **부작용**: 같은 그룹의 상위 기계가 `speedMultiplier`·`energyUseRate` 로만 구별된다.
화로 3종이 전부 속도 x1 이라 상위 기계를 만들 이유가 없어져 **x1 / x2·100 per s / x4·300 per s 로 벌려 뒀다(임시값).**

### N-1. 한 일

- [x] **다우징 로드를 만들 수 없어 게임이 시작조차 안 됐다** — 레시피 산출은 `dowsing_rod_t0` 인데
      `UndergroundPalette.DowsingTierOf` 는 `dowsing_rod` 만 인정. `ItemAliases` 로 흡수했다.
      실측 도달 범위 **아이템 16 · 기계 2 → 68 · 12**(코어 t0).
- [x] **중급 재단을 놓을 수 없었다** — 아이템 `Machine:Intermediate_altar` ≠ 블록 `Machine:IntermediateAltar`.
      전수 조사 결과 `blockId == itemName == blockName` 위반은 이 하나뿐이었고, 아이템 쪽을 맞췄다(별칭 안전망 2줄).
- [x] **재료 없이 무한 생산되던 t2 레시피 5개** — 정밀 세공기는 전력만 있으면 모터가 영원히 쏟아졌다.
      모터/베어링/프로펠러는 `강화 합금 ×10` 공통 + 구리판4 / 강철2 / 티타늄 주괴2 로 갈랐고
      (같으면 `SelectRecipe` 가 첫 것만 돈다), 업그레이드 모듈 2종은 노션 값(특수합금·강화 합금 ×200).
- [x] **2티어 관문 완화** — `UndergroundLootTable` 에 `bone_meal`(0.15 · 3~8개) 한 줄.
      실측 등급 0 방 한 판에 뼈 가루 10.1개 = 시멘트 5.1 → **콘크리트 1개에 약 4판 · 2티어 기계 1대에 약 8판**.
      `reinforced_concrete.tier` 1 → 2(목록엔 뜨는데 시멘트가 없어 못 만들던 것을 맞췄다).
- [x] **못 짓던 기계 3종에 건설 레시피** — 고전압 전기로(t2) · 지열 발전기(t1) · 변압기(t1).
- [x] **지열 발전기가 발전을 못 하던 것** — 막던 것이 셋이었다(§CLAUDE.md). 연료 없는 발전기는
      `fuelBurnRate` 를 그대로 발전량으로 쓴다. 실측 12/s.
- [x] **원유 채굴기를 펌프로 흡수** — 둘 다 지형에서 원유 1000 을 뽑는 레시피 하나뿐이었다.
      `정유기 → 증류기` 와 같은 절차(에셋 4개 삭제 + 별칭 3줄 + `MachineBlockFiller` 줄 제거 + JSON 4파일).
- [x] **마법 레시피 9종을 재단 전용으로**(사용자 지시) — 코어 조합기 중복을 전부 삭제했다.
      중급 재단(t1) `mana_chip` `mana_crystal` `enchanter` `apple_tree_seed` `resource_generator_t0_enhanced` ·
      초급 재단(t0) `resource_generator_t0` `enchanted_conductor_powder` `mana_shard_duplicate` ·
      **`tailoring_intermediate`**(중급 재단 건설 — `altar_intermediate` 가 이미 초급 재단에 같은 재료로 있었다.
      ⚠ 이름이 달라 처음 중복 조사에서 안 잡혔다). `altar_intermediate` 의 JSON tier 도 1 → 0 으로 맞췄다 —
      1 이면 초급 재단(btier 0)에 안 떠서 "중급 재단을 초급 재단에서 만든다" 가 깨진다.
      → **재단 계열은 자기 아래 단계에서 짓는다**: 코어 → 초급 → 중급(고급 재단만 고급 조합기).
      **폐쇄 수치가 한 개도 안 줄었다** — 재단 둘 다 코어 t0 에서 지어지기 때문이다.
      ⚠ `마법이 부여된 전도체 가루`(코어 1차 승급 재료)가 초급 재단에만 남아 **초급 재단이 승급의 관문**이 됐다.
      ⚠ `crafting.json` 의 `station` 필드는 임포터가 보지 않는다 — 적어 둬도 코어에 만들어지므로
      그 세 항목을 JSON 에서도 지웠다(정본은 `notion_magic.json`).
- [x] **죽은 에셋 정리** — 산출이 아예 없던 레시피 **11개**(발전기 9 · `geothermal` · `enchant`) +
      옛 변압기 처리 레시피 2개 + 가짜 연료 아이템 4종. `smelt_*` 4개의 뜻 없어진 tier 를 0 으로.
- [x] **JSON 동기화** — 7파일. ⚠ **`factory.json` 의 `transformer` 를 안 지웠으면** 임포터가
      `Incomplete/factory/` 에 같은 이름 레시피를 하나 더 만들었다(새 에셋은 `Incomplete/machines/`).
      같은 이유로 새 건설 레시피 3개는 `notion_machines.json`(→ `machines/`)에 넣었다.

**도달 범위(폐쇄 시뮬레이션, 지상 돌·마력석 + 지하 전리품에서 시작)**

| | 시작 | 끝 |
|---|---|---|
| 코어 t0 | 아이템 16 · 기계 2 | **70 · 14** |
| 코어 t1 | 17 · 2 | **116 · 28** |
| 코어 t2 | 19 · 2 | **138 · 39** |

건설 레시피 없는 기계 14종 → **9종**(1계열 4 · 2계열 4 · 핵발전소 — 전부 의도적 범위 밖) ·
`blockId == itemName == blockName` 어긋남 1 → **0** · 산출 없는 레시피 11 → **0** ·
재료가 빈 tier<=2 레시피 11 → **5**(전부 지형 추출이라 정상).

### N-2. 이번에 생긴 부채 · 남은 것

- [ ] ⚠ **화로 3종의 속도·전력 차등(x1 / x2·100 / x4·300)은 내가 정한 임시값**이다.
      티어 게이트가 사라져 셋이 구별되지 않게 된 것을 메운 것이라 실제로 돌려 보고 정해야 한다.
- [ ] ⚠ **업그레이드 모듈 200(특수합금·강화 합금)은 노션 값 그대로**다. `maxStack` 이 64라 인벤토리 4칸이고
      체감이 매우 크다 — 조정 대상.
- [ ] ⚠ **변압기는 지을 수 있는데 아무 일도 안 한다** — 전압 2풀(§A-6)이 미구현이라 가공 레시피가 0개다.
- [ ] **가공 레시피가 0개인 기계 5종** — 증류기 · 가스 발전기 · 수경 재배기 · 아이템 강화기 · **마법부여기**
      (`enchant` 를 지우면서 늘었다). "만들 수 있다" 는 됐고 "쓸 데가 있다" 가 다음 작업이다.
- [ ] **운석을 못 얻는다** — 분쇄는 이제 되는데 원석 획득처가 등급 2 지하방뿐이고 `dowsing_rod_t2` 가 없다.
- [ ] **`conductor_crystal`(전도체 결정)을 못 얻는다** — 1-1 추출이 유일한 획득처인데 1계열 추출기 4종이 아직 없다.
- [ ] **`저전압 전력`·`고전압 전력` 아이템의 참조가 0** 이 됐다(발전기 레시피를 지우면서).
      전압 2풀을 구현할 때 쓸 자리라 남겨 뒀다.
- [ ] `TODO.md §K-1` 의 *"`reinforced_concrete` 의 tier 가 0"* 은 사실이 아니었다 — **1 이었다**(이제 2).

---

## O. 2026-08-15 — 코어 조합대 정리 (완료)

**사용자가 정한 것** — 칼을 도구 체계로(옛 아이템 삭제) · 판은 재질 슬롯 하나로(**16재질 전부**) ·
압축기의 재질별 판 레시피는 유지 · 판은 **자원 탭** · 카테고리 재배치 4건.

### O-1. 한 일

- [x] **도구 탭 툴팁이 견본 이름을 읽었다** — `ToolPartRecipe` 는 아이콘용으로 `outputs[0]` 에 견본
      (`돌 망치 머리`)을 넣어 두는데 그것이 그대로 떴다. **`Recipe.ListName`(virtual)** 을 정본으로 만들고
      `ToolPartRecipe`→`kind`, `ToolRecipe`→`tool` 로 덮었다. 견본 `outputs` 는 그대로 둔다
      (아이콘·검색·적재 공간 검사가 그것을 본다).
- [x] **카테고리를 규칙 하나로 정본화** — `RecipeCategoryAssigner` 가 **돌릴 때마다 전부 다시 계산**한다.
      예전에는 `category != null` 이면 건너뛰어 JSON 임포터의 옛 값이 영영 남았다(그게 어긋남의 원인).
      ⚠ 재계산으로 바꾸니 **렌치가 도구 → 자원으로 역행**하는 것이 드러났다 —
      `WrenchItem` 은 `ToolItem` 을 상속하지 않고 `Items` 를 직접 상속한다(규칙에 추가).
      `Overrides` 표 9줄: 업그레이드 모듈 2·다우징 로드·횃불·전등·산성/유리 파이프·자동 조합기·공동 탐색기.
- [x] **칼을 도구 체계로** — 생성기 표에 줄 셋(`Kinds += blade` · `BuildDefinitions += knife` ·
      `BuildRecipes order += knife`). 아트는 `knife.png` 에 16재질 × `{m}_blade`·`{m}_knife` 가 이미 있었다.
      옛 아이템·레시피 6종 삭제 + `ItemAliases` + JSON 3파일 8항목.
      ⚠ `stone_blade`·`iron_blade` 는 **이름이 그대로 살아남아** 별칭이 필요 없다(타입만 바뀌었다).
- [x] **판을 재질 슬롯으로** — `plate` 부품 종류 + `Recipes/Tools/plate.asset`(재료 1개 · filter Any).
      코어의 `iron_plate`·`copper_plate`·`hammer_metal_plate` 3개 삭제 + JSON 4항목.
      ⚠ **기존 판 4종은 `m_Script` 만 바꿔 승격**했다(guid 보존) — **레시피 참조 44건, 끊긴 것 0건.**
      지우고 다시 만들었으면 전부 끊겼다. 바꾼 뒤 도메인 리로드 → 그다음 `Register All Assets`(§CLAUDE.md §2).
      ⚠ 생성기의 **존재 검사를 `itemName` 전역으로** 고쳤다 — 자기 폴더만 봐서 `iron_plate` 를 하나 더 만들 뻔했다.
- [x] **판은 자원 탭**(사용자 지시) — 16종을 예외표에 나열하지 않고 규칙 한 줄로 갈랐다:
      `ToolPartItem && kind == "plate"` → 자원. 재질이 늘어도 따라간다.

**코어 조합기 카테고리**

| | 전 | 후 |
|---|---|---|
| 기계 | 19 | **33** |
| 도구 | 7 | **10** |
| 블록 | 2 | **8** |
| 자원 | 38 | **9** |

부품 아이템 48 → **80개**(16재질 × 5종) · 부품 종류 3 → **5종** · 아이템 226 → **243개**.

### O-2. 이번에 생긴 부채 · 짚어 둘 점

- [x] **`metal_plate`(금속 판) 삭제** — 재질별 판 16종이 대신하면서 죽었다(남은 레시피가 압축기 하나인데
      재료 `금속 주괴` 를 얻을 수 없었다). 아이템 + `compressor_metal_plate` + JSON 2항목을 지우고
      `ItemAliases` 로 `금속 판`·`metal_plate` → `iron_plate` 를 이었다.
      ⚠ 이때 **두 단계 사슬 별칭 3줄**을 함께 고쳤다 — `금속판 → 금속 판`, `인바(invar) 판 → 인바 판`,
      `인바(invar) → 인바` 는 `Resolve` 가 한 단계만 풀어 **원래부터 안 통하고 있었다.** 최종 이름을 직접 가리킨다.
      ⚠ `금속`·`금속 주괴` 는 이제 참조 0인 고아다(지우지 않고 남겼다).
- [x] **`칼` 요구를 에셋에 반영** — `crank`(돌2 + 칼) · `dowsing_rod`(돌5 + 칼), 내구도 1씩.
      JSON 이 오래전부터 적어 두었으나 반영된 적이 없었다. 순환 없음(칼 = 막대[돌1] + 칼날[돌2]).
- [ ] **`stone_plate`·`wood_plate` 그림이 없다.** 돌 판은 만들 수는 있어 회색으로 나온다.
- [x] **`망치` 요구도 에셋에 반영** — `bucket` `item_pipe` `liquid_pipe` `gas_pipe`
      `magic_shard_duplicate`(초급 재단) · `dowsing_rod`(칼과 함께), 내구도 1씩.
      순환 없음 — 부품 5종과 도구 4종의 레시피는 **하나도 도구를 요구하지 않는다**(확인함).
      → 0티어 시작 순서가 못박혔다: 돌 → 막대·칼날·망치 머리 → **칼·망치** → 크랭크·양동이·다우징 로드.
- [ ] **판을 만드는 길이 둘이다**(수동=조합대 재질 슬롯 · 자동=압축기). 의도된 이중 경로다.
- [ ] 폐쇄 시뮬레이션 스크립트가 **재질 슬롯 레시피를 견본 하나로만 세고 있었다** — 이번에 고쳤다
      (종류 × 재료를 가진 재질만큼 센다). **그래서 이번 수치는 이전 보고와 직접 비교할 수 없다.**
      새 기준: 코어 t0 **110/15** · t1 **163/28** · t2 **190/39**.

---

## P. 2026-08-18 — 플레이어 방향 조명 (빔 + 오라) · 방향별 정지 자세 (완료)

세계가 통째로 동굴인데 플레이어 빛이 360° 원형이라 **어디를 보든 똑같이 밝았다.**
보는 쪽으로 빛이 나가게 하고, 정지 자세도 마지막 이동 방향으로 남게 했다.

### P-1. 한 일

| | 무엇 |
|---|---|
| `PlayerAnimation` | **`Facing`(월드 방향) 이 방향의 정본.** 입력이 0 이 되어도 `moveX/moveY` 에 0 을 쓰지 않고 마지막 방향을 남긴다 |
| `Female.controller` | Idle 이 `AnimationClip` → **`BlendTree(SimpleDirectional2D, moveX/moveY)`**. 전이·파라미터·Blink 상태 변경 0 |
| 새 클립 3장 | `FemaleIdle{Backward,Leftward,Rightward}` — **걷기 클립의 2프레임** |
| `LightingPalette` | `Beam*`(각도 포함) · `Aura*` 두 벌. 옛 `Player{Inner,Outer}Radius`·`PlayerIntensity` 는 `Beam*` 이 물려받음 |
| `MapLighting` | `playerAura` 필드 추가. 빔에는 **각도까지** 밀어 넣는다 |
| `GameRig.prefab` | `PlayerAura` 신규(360° · `normalMapQuality` 2) · `PlayerLight` 는 콘(0/128) + `PlayerLightAim` |
| `PlayerLightAim`(신규) | `Facing` → `Atan2 - 90°` 로 **즉시 스냅**. 방향이 바뀐 프레임에만 `localRotation` 을 쓴다 |
| `MapTest.unity` | PlayerLight 오버라이드 **15줄 제거**(-68줄). 콘이 MapTest 에만 있어 **지하에서는 원형**이던 것을 고쳤다 |

**세이브는 v12 그대로** — 방향은 저장하지 않는다(불러오면 정면으로 선다).

### P-2. 이번에 생긴 부채 · 짚어 둘 점

- [ ] **방향별 Idle 3장은 걷기 2프레임을 빌린 임시 포즈다.** 스프라이트 시트에 옆·뒤 정지 그림이
      아예 없다(`Female_normal1~5` 는 정면뿐). 전용 아트가 오면 **클립 3장만 갈아 끼우면**
      컨트롤러·코드는 그대로다.
- [ ] **Blink 상태가 사실상 죽어 있다.** `FemaleBlink.anim` 은 빈 클립이고 `FemaleNormal` 이
      이미 5프레임으로 깜빡인다. 지금은 **정면일 때만 트리거를 쏘게 막아** Write Defaults 로 스프라이트가
      튀는 것을 피했을 뿐이다 — 상태를 아예 지우는 것이 맞는지 정해야 한다.
- [ ] **대각선에서 애니메이션과 빛이 살짝 다르다.** 빔은 정확한 각도로, 스프라이트는 우세 방향
      한 장으로 간다(블렌드 가중 0.50). 의도지만 눈에 걸리면 빔도 4방향으로 자르면 된다.
- [ ] **빔에 그림자가 켜져 있어 좁은 통로에서 시야가 확 좁아진다.** 답답하면 오라 반경(1.6)을
      먼저 키운다 — 빔 각도를 넓히면 방향성이 사라진다.
- [ ] `Global Light 2D` 의 `m_Intensity: 1` **씬 오버라이드가 아직 MapTest 에 남아 있다.**
      런타임에는 `MapLighting.Apply()` 가 0.18 로 덮으므로 무해하지만, 에디트 모드에서만
      두 씬이 다르게 보인다.
- [ ] `MapGenerator.ApplyFootprintCollider` 는 **호출만 껐고 메서드는 남아 있다**(죽은 코드).

### P-3. 노멀맵 — 남은 범위 (2026-08-17 작업의 후속)

- 지금 `_NormalMap` 이 붙은 것은 **`Assets/Asset/MachineImages/*.png` 8장뿐**이다.
- 남은 것: **블록**(`BlockImages`) · **파이프**(`pipes.png`) · **타일 아틀라스** · **플레이어**.
  배선 방식은 같으므로 `NormalMapGenerator` 의 입력 폴더만 늘리면 된다 —
  ⚠ 단 **타일맵은 `Tile` 에셋을 거치므로 별도 확인**이 필요하다.
- **손그림으로 교체할 때는 `Normals/{이름}_n.png` 를 덮어쓰기만 하면 배선은 그대로다.**

---

## Q. 2026-08-19 — 자원 생성기 (입력 없이 1초에 1개) (완료)

`0티어 자원 생성기` 는 에셋·아이템·프리팹·건설 레시피까지 다 있는데 **한 개도 만들어 내지 않고 있었다.**
1·2티어는 기계가 없어 `extract_ore`(마력석)·`extract_meteorite`(운석)가 주인 없이 떠 있었다.

### Q-1. 한 일

| | 무엇 |
|---|---|
| `MachineInstance.CanRun` | 런타임 `inputSlotCount == 0` 인 기계에서만 **무입력 레시피를 허용**. `RecipeSolver.HasInputs` 는 **손대지 않았다** |
| 그룹 분리 | `ResourceGenerator` → `ResourceGenerator0 / 1 / 2` (그룹마다 레시피 정확히 1개) |
| 무전력 | 4종 모두 `isUseEnergy 0` · `energyUseRate 0` · `maxEnergyAmount 0` · `fuelBurnRate 0` |
| 신규 기계 | `Machine:ResourceGenerator1`(마력석) · `Machine:ResourceGenerator2`(운석) — 블록·아이템·프리팹 |
| 레시피 배선 | `extract_ore` → t1 · `extract_meteorite` → t2, 둘 다 `tier` 를 1·2 → **0** 으로 정리 |
| UI | `ResourceGenerator0_UI.prefab`(`BlastFurnace_UI` 에서 입력 2칸만 제거) 한 장을 **4종이 공유** |

**세이브는 v12 그대로.** 실측:

```
| 기계                    | 그룹               | 레시피 | 전력   | 10초 산출  |
| 0티어 자원 생성기        | ResourceGenerator0 | 1     | 무전력 | 돌 x10     |
| 0티어 자원 생성기(강화)  | ResourceGenerator0 | 1     | 무전력 | 돌 x20     |
| 1티어 자원 생성기        | ResourceGenerator1 | 1     | 무전력 | 마력석 x10 |
| 2티어 자원 생성기        | ResourceGenerator2 | 1     | 무전력 | 운석 x10   |
속도 모듈 16개 -> 초당 5개    ·    [회귀] rod 를 빈 슬롯으로 CanCraft = False
```

### Q-2. 이번에 생긴 부채 · 보류한 것

- [ ] **1·2티어 건설 레시피가 없다**(사용자 보류) — 아이템 목록(`P`)·콘솔로만 얻는다.
      **이것이 지금 유일한 해금 장치**이므로, 정하기 전까지 마력석·운석은 사실상 잠겨 있다.
- [ ] **`0티어 자원 생성기(강화)` 를 어떻게 할지 미정**(사용자 추후 결정). 모듈이 속도를 올려 주므로
      역할이 겹치는데, 지금은 `speedMultiplier 2` 라 모듈과 곱해져 최대 10개/초가 된다.
- [ ] **전용 아트가 없다** — t1·t2 는 t0 의 그림·아이콘(`assetPlaceHolder`)을 그대로 쓴다.
- [ ] ⚠ **밸런스가 크게 움직였다.** 마력석·운석은 원래 지하 등급 1·2 방이 유일한 획득처인데
      `dowsing_rod_t1/t2` 가 없어 **닿을 수 없는 자원**이었다. 건설 레시피가 붙는 순간
      1·2계열 분쇄 사슬이 통째로 열린다 — 그때 값을 조인다.
- [ ] ⚠ **`펌프`는 이번 수정으로도 안 돈다.** `extract_water`·`extract_oil` 도 무입력 레시피지만
      펌프는 `in0/out0/fuel0 · AllowsZeroSlots false` 라 `ApplyConfig` 의 **3/6 폴백**에 걸려
      런타임 `inputSlotCount` 가 3이 된다. 출력 칸을 1로 두거나 `AllowsZeroSlots` 를 열어야 한다. **별건.**
- [ ] **지형 요구는 구현하지 않았다.** Notion 정본(`extraction.json`)은 "일반/광석/운석 지형에 설치" 지만
      그런 지형 타일이 게임에 없다. **기계 티어가 산출을 정한다**(2026-08-19 사용자 결정).
- [x] `upgradeSlotCount` 는 **YAML 에 줄이 없어도 C# 초기값 2 로 읽힌다**(0 이 아니다).
      `size` 와 같고 CLAUDE.md §2 의 "새 필드는 0 으로 읽힌다" 와는 다르다 — 필드마다 다르니 **실측할 것.**

---

## R. 2026-08-19 — 웅덩이 계층 (지하 입구 · 석유 웅덩이 · 물 · 펌프) (완료)

셋이 뿌리 하나에 걸려 있었다 — **"바닥에 고인 것" 을 표현할 자리가 없었다.**
지하 입구는 런타임 사각형이라 세이브에 안 남았고, 공동 탐색기는 표에 줄이 없어 무반응이었고,
펌프는 발밑을 보는 코드가 아예 없었다.

### R-1. 한 일

| | 무엇 |
|---|---|
| **오버레이 계층** | `Chunk` 에 희소 사전 + `FloorOverlay` 타일맵(**sortingOrder 115**). **세이브 v13** |
| | 바닥 타일을 갈아 끼우지 않는다 → 지형이 비쳐 보이고 되돌릴 것도 없다 |
| 블록 5종 | `overlay:hole0/1/2`(hole) · `overlay:oil`(oilhole, crude_oil) · `overlay:water`(waterhole, water) |
| `WorldMap.FluidAt` | **"이 칸에 무엇이 고여 있나" 의 정본 하나.** 양동이와 펌프가 같은 함수를 본다 |
| 지하 입구·출구 | `UndergroundPortal.cs` **삭제**. 판정은 `PlayerInteraction.TryUseHole` 한 곳 · **1회용 유지** |
| 지하 물 | `floor:water` → `overlay:water` (방 바닥 위에 겹친다) |
| 공동 탐색기 | 등급 1 · 발견 25% · 그중 **20% 는 석유 웅덩이** · **내구도 30** |
| 내구도 구현 | `Items.initialDurability` + `ToolInstance` 재사용. 부착은 `RecipeSolver.AddItems` 하나 |
| 펌프 | `ApplyConfig` 에 유체 탱크 추가(3/6 폴백 해소) · `CanRun` 이 발밑 유체로 레시피 선택 · 40/s |

**실측**

```
세이브       v13 왕복 OK · v12 세이브 호환 OK(오버레이 0, NRE 없음)
탐색기       2000회 → 발견 26.2%(기대 25) · 그중 석유 19.7%(기대 20) · 내구도 정확히 30회
펌프         맨바닥 0 / 석유 crude_oil 8000 / 물 water 8000 / 전력 0 → 0
웅덩이       8000 뽑고 비우기 ×3 — 오버레이 그대로(무한)
회귀         자원 생성기 4종 그대로 · 조합대 도구 부품 6개 그대로
```

### R-2. 이번에 생긴 부채 · 짚어 둘 점

- [ ] ⚠ **석유 웅덩이는 영구·무한이다.** 공동 탐색기 하나(내구도 30)로 평균 7~8번 발견하고 그중 20% 가
      웅덩이라 **한 개로 웅덩이 약 1.5개**다. 원유 → 증류기 사슬이 통째로 열리므로,
      세다 싶으면 `UndergroundPalette.OilPoolChanceFor` 한 줄만 낮추면 된다.
- [ ] **`floor:water` 는 지우지 않았다.** 옛 세이브에 타일로 박혀 있고 `FluidAt` 이 바닥도 보므로
      그대로 돈다. 지하맵만 오버레이로 옮겼다 — 통일할지는 별건.
- [ ] **`overlay:hole*` 3종이 그림을 공유한다**(전부 `hole.png`). 등급이 눈으로 안 구별된다.
- [ ] ⚠ **`overlay:` 접두사는 `Chunk.IsWall`/`IsFloor` 어디에도 안 걸린다** — 의도다.
      통행을 막지도, 캐어지지도, 배치를 막지도 않는다(펌프가 그 위에 서야 한다).
- [ ] **공동 탐색기의 `maxStack` 이 64 → 1 로 내려갔다.** 이미 여러 개 들고 있던 세이브의 스택은
      인스턴스가 없어 예전처럼 동작하고, 새로 만든 것부터 1칸씩 쌓인다.
- [ ] **`plantedAtUtcTicks` 가 세이브에 두 번 쓰이고 두 번 읽힌다**(`WorldMap.cs:232-233`, `:404-406`).
      대칭이라 포맷은 깨지지 않지만 청크당 8바이트가 낭비된다 — 별건.
- [ ] 전용 아트 없음: `hole`·`oilhole`·`waterhole` 는 그대로 쓰지만 **펌프 자체의 월드 그림**은
      기존 것을 쓴다(플레이스홀더 아님).

---

## S. i 키가 기계 UI 를 못 닫던 문제 (2026-08-19)

증상은 "상자·아이템 저장소 UI 가 i 키로 안 닫힌다" 였지만 **기계 47종 전부**에 해당했고,
조건은 **씬을 한 번 갈아 끼운 뒤**(= 지하를 다녀온 뒤)였다.

| | |
|---|---|
| 원인 | `InventoryToggle` 은 씬을 넘어 사는 `PlayerInventory` 에 붙어 있는데, `Start` 에서 `MachineInteraction`(GameRig 안 = 씬과 함께 죽는다)을 **한 번만** 찾아 캐시했다. 그 `Start` 는 다시 불리지 않으므로 참조가 유니티 가짜 null 로 남는다 |
| 증상 | `Toggle` 이 기계 분기를 건너뛰어 **인벤토리만 닫히고 기계 창은 남는다.** `isAnyUIOpen` 이 계속 참이라 `PlayerMove` 가 이동을 막아 **조작이 통째로 잠긴다** |
| 수정 | `InventoryToggle.Machine` 프로퍼티 — **캐시가 죽어 있으면 그때마다 다시 찾는다**(`PowerLinkMode.machineInteraction` 과 같은 규약). `Start` 삭제 |
| 전수조사 | 런타임 스크립트 중 **씬 오브젝트를 캐시하는 지속 오브젝트는 `InventoryToggle` 하나뿐**이었다. 나머지 `Find*` 호출부(`PowerLinkMode` · `ItemBrowser` · `CommandConsole` · `TooltipUI` · `InventorySlot`)는 전부 GameRig 안이라 캐시 수명이 씬과 같다 |

**실측**

```
수정 전  씬 전환 전 : i → Machine=False Inv=False   (닫힘)
        지하 왕복 후: i → Machine=True  Inv=False   (기계 창이 남는다)
수정 후  씬 전환 전 : i → Machine=False Inv=False   (진짜 키 입력으로 확인)
        지하 왕복 후: i → Machine=False Inv=False   (상자·아이템저장소·화로·코어조합기·저장터미널 5종)
        InventoryToggle.Machine 재탐색 → alive:TestPlayer
컴파일 · 콘솔 에러 0 · 경고 0
```

### S-2. 검증하며 알게 된 것 (테스트 환경 함정)

- [ ] ⚠ **`EditorApplication.Step()` 을 쓰면 에디터가 일시정지 상태로 남는다.** 그 뒤에 큐에 넣은
      합성 키 입력은 액션에 닿지 않아 **"입력이 죽은 것처럼" 보인다.** `isPaused = false` 를
      **별도 호출로** 돌려놓아야 다음 호출부터 정상이다(같은 호출 안에서 풀면 안 먹는다).
- [ ] ⚠ **씬을 `LoadScene` 으로 갈아 끼운 뒤에는 `InputSystem.QueueStateEvent` + `Update()` 조합이
      액션을 깨우지 못한다** — 그 자리에서 새로 만든 `InputActionMap` 도 안 뜨는 것으로 확인했다.
      게임 버그가 아니라 하네스 문제이므로, 그 뒤 검증은 **핸들러를 직접 호출**해서 한다.
- [ ] ⚠ **프레임이 안 도는 동안에는 `Start` 가 실행되지 않는다.** 그래서 씬 로드 직후를 들여다보면
      `UIManager.UIs` 에 "Inventory" 가 없고 `InventoryUI.slots` 가 null 이다 —
      **이것을 버그로 오인하지 말 것.** `Step()` 몇 번이면 채워진다.

---

## T. 사운드 3종 (2026-08-19)

게임에 오디오가 한 줄도 없던 상태에서 첫 소리 셋을 넣었다. 자세한 규약은 `CLAUDE.md §사운드`.

| | |
|---|---|
| 발소리 | `MainBlock.footstepSound`(정본) → `WorldMap.FootstepAt`(조회) → `PlayerFootsteps`(루프 재생) |
| 조합음 | `SfxPlayer.PlayCraft` 한 곳. 조합대는 성공 시에만, 기계는 `boundUI != null` 일 때만 |
| 새 파일 | `Scripts/Audio/SfxPlayer.cs` · `Scripts/Player/PlayerFootsteps.cs` (둘 다 `create_script`) |
| 프리팹 | `GameRig/SfxPlayer` 신설 · `GameRig/TestPlayer` 에 `AudioSource` + `PlayerFootsteps` |

**실측**

```
클립      WalkOnStone 2.541s(8걸음 루프) · WalkOnDirt 2.101s(6걸음 루프) · CraftSound 2.020s(단발)
         셋 다 48kHz mono · DecompressOnLoad · Vorbis · preloadAudioData 0 → 1 로 바꿈
조합대    성공 1번 / 실패(재료 0개) 0번 · 인벤토리 스냅샷 복구 확인(사용 칸 15 → 15)
기계      자원 생성기 UI 열림 1번 / UI 닫힘 0번
발소리    지상 floor:stage1 → WalkOnStone · 지하 1등급 floor:dirt → WalkOnDirt · 왕복 시 클립 교체
         지하 방 7×7: 물웅덩이 3칸 무음 · 출구 오버레이 1칸 무음 · 흙 45칸 WalkOnDirt
         멈추면 즉시 Stop · 인벤토리 열고 WASD → IsMoving False, 무음
```

### T-2. 남은 것 · 짚어 둘 점

- [ ] ⚠ **완성음에 최소 간격이 없다**(사용자 결정). 자원 생성기 UI 를 열어 두면 초당 1번,
      속도 모듈을 채운 강화형이면 **초당 10번** 겹친다. `SfxPlayer.PlayCraft` 에 한 줄이면 되고
      호출부는 안 바뀐다.
- [ ] **걷는 속도와 발소리 박자가 안 맞는다.** 이동 속도 5 vs 클립 박자 초당 약 2.8~3보.
      한 걸음짜리 클립으로 바꾸면 `PlayerFootsteps` 재생 규칙만 갈아 끼우면 된다
      (`MainBlock.footstepSound` · `WorldMap.FootstepAt` 은 그대로).
- [ ] ⚠ **`PlayerAnimation` 은 입력을 직접 읽어 UI 가 열려 있어도 "걷는 중"이 된다** —
      인벤토리를 켜 두고 WASD 를 누르면 **제자리에서 걷기 애니메이션이 돈다**(기존 동작).
      발소리는 `PlayerMove.IsMoving` 을 보므로 여기서 둘이 갈린다. 맞출지는 별건.
- [ ] **소리가 없는 것들**: 채굴 · 배치 · 아이템 줍기 · UI 클릭 · 지하 진입 · 파이프 · 발전기 가동음.
- [ ] **볼륨 설정 UI·마스터 볼륨·오디오 믹서가 없다.** 두 컴포넌트의 직렬화 필드로만 조절한다.
- [ ] `overlay:oil`·`overlay:hole*` 도 무음이라 **웅덩이를 건널 때 발소리가 한 칸 끊긴다** — 의도지만
      전용 클립(첨벙·텅)이 생기면 그 에셋에 꽂기만 하면 된다.

### T-3. 검증 환경 함정 (§S-2 에 이어)

- [ ] `EditorApplication.Step()` 은 에디터를 **일시정지로 남긴다.** 그 상태에서는 합성 키 입력도
      `AudioSource` 재생도 정상 판정이 안 된다. `isPaused = false` 를 **별도 호출로** 돌려놓을 것.
- [ ] `AudioSource.isPlaying` 은 `PlayOneShot` 에도 참이 되고 `Stop()` 으로 함께 멎는다 —
      "몇 번 울렸나" 는 **테스트마다 `Stop()` 하고 다시 보는 것**으로 셌다.

---

## U. 아이템 도달성 조사 · 레시피 두 개 교환 (2026-08-19)

에셋 542개(아이템 260 · 레시피 159 · 기계블록 54 …)를 파싱해 **산출 그래프의 도달 닫힘**을 계산했다.
씨앗: 지상 채굴(`stone`·`manastone`) · 코어 조합기 · 다우징 로드를 만들 수 있으면 지하 전리품 7종과 물 ·
공동 탐색기를 만들 수 있으면 석유. 확률 산출은 `ExtractionTable` 배수가 0 이 아닌 **등급의 추출기를
실제로 지을 수 있을 때만** 인정했고, 무입력 유체 레시피(펌프)는 발밑에 그 유체가 있을 때만 돈다고 봤다.
콘솔·아이템 목록(P)·`TestItemGiver` 같은 디버그 경로는 뺐다.

**막힌 뿌리 다섯**

| # | 뿌리 | 뒤에 매달린 것 |
|---|---|---|
| 1 | **0-2 등급 순환 교착** — `extractor02` 재료에 `nickel_ingot`, 니켈은 0-2 에서 열린다 | 니켈·오스뮴·납·은·유황·티타늄·알루미늄 계열 전부, 정밀 세공기, 업그레이드 모듈, 용광로, 강철, 인바, 강화/특수합금, 핵 사슬 |
| 2 | **1계열 추출기 4종 건설 레시피 없음** | 전도체 결정 → 공명 결정 → 저장 셀 8종·저장 기판·공명 칩·데이터 케이블·저장망 3종·고급 재단 |
| 3 | **운석 획득처 없음**(2계열 추출기·`ResourceGenerator2` 레시피 없음, `dowsing_rod_t2` 없음) | 운석 · 운석 조각/가루/미세가루 · 에너지 결정 |
| 4 | **게임에 나무가 없음**(`ToolMaterial.wood.sourceItem` 비어 있음) | 나무 부품 5종 · 나무 씨앗 · 사과나무 씨앗 · 농사용 흙 |
| 5 | **증류기 레시피 0개** | 석유(`petroleum`) |

**완전 고아 7종** (산출도 소비도 없다): `metal` `metal_ingot` `low_voltage_power` `magic_powder`
`surface_powder` `osmium_ingot` `thorium_ingot`.
⚠ 뒤의 둘은 **원석은 추출표에 있는데 제련 레시피가 없다** — 원석 13종 중 이 둘만 소비처가 0이다.

**이번에 바꾼 것**

| 파일 | 변경 |
|---|---|
| `Recipes/Incomplete/machines/laser_processor.asset` | `invar_plate ×8` → **`iron_plate ×8`** |
| `Recipes/Incomplete/machines/precision_fabricator.asset` | `iron_plate ×50` → **`invar_plate ×50`** |
| `StreamingAssets/Recipes/notion_machines.json` | 같은 두 줄(`인바 판`↔`철판`) |
| `StreamingAssets/Recipes/crafting.json` | `laser_processor` 의 `인바 판` → `철판` |

⚠ **JSON 을 같이 고쳐야 한다** — 안 그러면 `Import JSON Recipes` 가 되돌린다(§7 의 함정).
⚠ 한글 이름은 **타이핑하지 않고 파일에서 뽑아 맞바꿨다**(NFC/NFD 사고 방지).
⚠ `laser_processor` 는 **`crafting.json` 과 `notion_machines.json` 두 곳에 중복 정의**돼 있다
(전자에만 `"tools": ["드라이버"]` 가 있다). 둘 다 고쳤지만 정본을 하나로 합치는 것이 맞다.

**실측**

```
바꾸기 전   아이템 260 · 만들 수 있음 113 · 없음 147
바꾼 뒤     아이템 260 · 만들 수 있음 116 · 없음 144
새로 열림   Machine:LasorProcessor · cavity_scanner · 원유(담긴 그릇)
            → 공동 탐색기가 열려 <b>석유 웅덩이 발견 경로가 실제로 살아났다</b>
컴파일 · 콘솔 에러 0 · 경고 0
```

### U-2. 남은 것

- [ ] ⚠ **교착은 옮겨졌을 뿐 안 풀렸다.** 레이저 가공기는 고리에서 빠졌지만
      `Extractor02 ← nickel_ingot ← raw_nickel_ore ← Extractor02` 가 **자기 자신을 향한 고리**로 남는다.
      끊는 방법 셋: ① `extractor02` 재료에서 `nickel_ingot` 제거 · ② `ExtractionTable` 에서 니켈을
      0-1 로 내림 · ③ 니켈에 다른 획득처(지하 전리품 등) 추가. **사용자 결정 필요.**
- [ ] **정밀 세공기가 `invar_plate ×50` 이 됐다** = 인바 50 = 니켈 50 + 철 50 수준. 교착이 풀린 뒤
      실제로 만들어 보고 숫자를 다시 볼 것.
- [ ] `raw_osmium_ore`·`raw_thorium_ore` 의 제련 레시피가 없다(주괴 아이템은 있다).
- [ ] 기계 아이템 7종이 레거시 **`Machine : Items`** 클래스를 쓴다
      (`CoreCrafter` `AlloySmelter` `BioIncubator` `Compressor` `ElectricPulverizer` `Electrolyzer`
      `LasorProcessor`). 동작엔 지장 없지만 클래스로 아이템을 훑는 도구가 빠뜨리기 쉽다 — 실제로 걸렸다.

### U-3. 니켈 전리품으로 교착 해소 (2026-08-19, 사용자 작업)

`UndergroundLootTable` 에 `new Row("nickel_ingot", 0.10f, 1, 3, 1)` 추가.
**`minTier 1`** 이라 공동 탐색기(= 1등급 탐지기)로 연 방에서만 나온다 — 앞 단계에서 레이저 가공기를
고리에서 빼 공동 탐색기가 열렸기 때문에 성립한다. **두 변경이 짝이다.**

```
도달 가능 아이템   116 → 190 / 260   (못 만드는 것 144 → 70)

지하 방 200판 생성 실측
  등급 0   니켈이 나온 판 0/200                      (minTier 1 이 제대로 걸린다)
  등급 1   nickel_ingot 총 403개 · 판당 2.02
           니켈이 나온 판 133/200
  다른 행은 등급 0/1 이 사실상 동일(마력파편 19.4/18.7 · 철 13.7/13.8 · 뼈가루 11.3/10.9 …)
           → 니켈 행을 맨 뒤에 붙여 <b>기존 행의 실질 확률이 안 줄었다</b>
컴파일 · 콘솔 에러 0
```

⚠ **표에 적힌 0.10 은 명목값이고 실질은 약 2.9%다** — 칸마다 위에서부터 훑어 처음 맞은 행만 놓는데
니켈이 맨 마지막 줄이라 앞의 7행이 전부 빗나가야 차례가 온다(∏(1-p) ≈ 0.287 × 0.10). 판당 2개가
그래서 나온 숫자다. 더 흔하게 하려면 확률을 올리거나 줄을 위로 올려야 하고, **위로 올리면
아래 행들이 조용히 줄어든다**(파일 주석의 경고 그대로).

**니켈 소요량** — `alloy_invar`(니켈1 + 철2 → 인바2) · `compress_invar_plate`(인바1 → 인바판1) ·
`extractor02`(컴퓨터칩2 + 0-1추출기1 + **니켈1**) · `precision_fabricator`(레이저가공기1 + **인바판50** + 콘크리트2).
→ **정밀 세공기 1대 = 니켈 약 26개 ≈ 1등급 방 13판 ≈ 공동 탐색기 2개.**

### U-4. 아직 남은 70개의 뿌리 (변동 없음)

| 뿌리 | 막힌 것 |
|---|---|
| **1계열 추출기 4종 건설 레시피 없음** | 전도체 결정 · 루비 · 사파이어 · 다이아 · 리튬/토륨 원석 · 리튬 주괴 · 공명 결정 → 공명 칩 · 데이터 케이블 · 저장 셀 8종 · 저장 기판 · 저장망 3종 · 리튬/토륨 부품 10종 |
| **운석 획득처 없음** | 운석 · 조각/가루/미세가루 · 에너지 결정 · 2계열 추출기 4종 · 자원 생성기 1·2티어 |
| **나무가 없음** | 나무 부품 5종 · 나무 씨앗 · 사과나무 씨앗 · 농사용 흙 · 프로토타입 2종 |
| **증류기 레시피 0개** | 석유 |
| **제련 레시피 없음** | `osmium_ingot`(원석은 이제 얻을 수 있다) · `thorium_ingot` + 오스뮴 부품 5종 |
| **완전 고아** | `metal` `metal_ingot` `low_voltage_power` `magic_powder` `surface_powder` |

⚠ **오스뮴이 새 항목이다** — 0-2 가 열리면서 `raw_osmium_ore` 는 얻을 수 있게 됐는데
**제련 레시피가 없어** 주괴와 부품 5종이 그대로 막혀 있다. 원석 13종 중 오스뮴·토륨만 그렇다.

---

## V. 연료 체계 개편 (2026-08-20)

기계별 허용 연료 · 연료별 발전량/지속시간 · 가솔린. 규약은 `CLAUDE.md §연료`.

| 단계 | 무엇 |
|---|---|
| 1 | `petroleum`(석유) → `gasoline`(가솔린) 개명(guid 유지) · 증류기 `원유 1000 → 가솔린 500`(craftTime 2) · 증류기 `energyUseRate` 0 → 100(폴백 제거) |
| 2 | `FuelTable`(static 정본) · `MachineInstance.AcceptsFuel` · `IItemContainer.AcceptsItem` · 세이브 v14 `burnRate` |
| 3 | 가스 발전기 유체 연소(연료칸 0 · 입력 탱크 1 · 2000) · `PipeRouter.TargetTanks` 발전기 분기 · `Generator_UI` 에 입력 유체 바 |

**실측**

```
[가솔린]
  유체 8종 유지 · GetFluid(gasoline)=가솔린 · GetItem(petroleum)→gasoline(별칭) · FindItem("석유")→gasoline
  증류기 crude_oil 1000 -> gasoline 500 · 전력 1000 -> 750 (100/s x 2.5초)

[화이트리스트]            coal  brown_coal  nuclear_fuel_rod
  화력 발전기               O       O            X
  핵발전소                  X       X            O
  가스 발전기               X       X            X      (유체만)
  화로                     O       O            O      (표에 없음 = 예전 그대로)

[연료별 실측]
  화력+석탄     20.0초 · 총 400   · 20.0/s
  화력+갈탄     10.0초 · 총 200   · 20.0/s
  핵+연료봉     60.0초 · 총 30000 · 500.0/s
  가스+원유 1000   20.0초 · 총 500  · 25.0/s      (2000 넣으면 40초 · 1000)
  가스+가솔린 1000 20.0초 · 총 2000 · 100.0/s
  가스+물 1000     0.0초 · 탱크 1000 그대로       (거부)
  파이프 수용   원유 O · 가솔린 O · 물 X

[세이브]
  v14 왕복 3450바이트 · 남은 0 · burnRate 20 복원
  v13 폴백 record.burnRate=0 -> ActiveBurnRate 가 기계 fuelBurnRate 로 떨어진다

[회귀]
  지열   무연료 분기 유지 · 1초에 12
  가스   버퍼 가득이면 안 태운다(탱크 2000 그대로 · 가동 false)
  핵     연료봉 2개 -> 사용후 핵연료봉 x2 (찌꺼기 훅 그대로)
  화로   석탄 20초 · 갈탄 10초 · 핵연료봉 500초 (개편 전과 동일)
컴파일 · 콘솔 에러 0 · 경고 0
```

### V-2. 계획에서 바뀐 것 · 남은 것

- [ ] **계획의 `FuelSlotUI` 새 스크립트는 안 만들었다.** 연료 슬롯 전용 빌딩블록 프리팹이 없어
      (팩토리가 `MachineSlot.prefab` 을 공유한다) 스크립트를 갈아 끼우면 <b>모든 슬롯</b>이 바뀐다.
      대신 `SlotCapacity` 와 같은 방식으로 **컨테이너에 묻게**(`IItemContainer.AcceptsItem`) 했더니
      기존 UI 프리팹 전부에 저절로 적용됐다. 구현체 4종에 한 줄씩 추가.
- [ ] ⚠ **`FuelTable` 의 화력·핵 수치는 개편 전 값(20/500) 그대로 두었다** — 사용자가 직접 조정한다.
      에셋의 `fuelBurnRate`(화력 200 · 핵 3000)는 **표가 이겨서 그 두 기계에서는 안 쓰인다.**
      바꾸려면 `FuelTable.Rows` 의 `rate`/`seconds` 를 고칠 것.
- [ ] **가스 발전기는 파이프 전용이다** — 입출력 아이템 칸이 0 이라 양동이 교환이 안 걸린다.
      양동이로도 넣으려면 `inputSlotCount`/`outputSlotCount` 를 1 씩 주면 된다(코드 변경 없음).
- [ ] **가스 발전기 UI 를 `Generator_UI` 로 배선했다**(전에는 `uiPrefab` 이 비어 기본 패널로 떨어졌다).
      입력 유체 바를 연료 바 위에 코드로 얹었으므로 **위치는 팩토리 창에서 다듬는 것이 낫다.**
- [ ] `GasGenerator.asset` 에 남아 있던 옛 필드명 `inputGasSlotCount`/`outputGasSlotCount`/`maxGasAmount` 를
      현재 이름으로 갈아 끼웠다. ⚠ **`maxGasAmount` 는 float→int 라 `FormerlySerializedAs` 가 안 걸려**
      값이 0 으로 읽히고 있었다. 같은 옛 줄이 **다른 에셋 15개**(추출기 12종 · 합금 재련기 · 변압기 ·
      Extractor00Plus)에 남아 있지만 **전부 0 이고 그 기계들은 탱크가 없어** 지금은 무해하다 —
      그 기계에 탱크를 줄 때 `maxGasAmount` 만 조용히 0 으로 읽히는 것에 주의할 것.
- [ ] 화로 화이트리스트는 안 걸었다(지시는 발전기 셋뿐) — 핵연료봉을 화로에서 500초 태울 수 있다.

---

## W. 전송 모드 화면 이동 · 첫 시작 곡괭이 (2026-08-20)

| | 무엇 |
|---|---|
| 전송 모드 | `PowerLinkMode` 에서 이동 키로 화면을 민다. `CinemachineCamera.Follow` 를 떼고 vcam 트랜스폼을 직접 옮긴다 · `Move` 액션만 도로 켠다 · 발전기 중심 정사각형(`powerRange + panMargin 3`)에 가둔다 |
| 첫 시작 지급 | `StartingInventory.Grant` (새 파일) — 세이브가 없을 때 `PlayerSave.Load` 가 한 번 부른다. 돌 곡괭이 1개를 **손에 든 칸**에 넣는다 |
| 곁들여 | `ToolDictionary.Tools` 접근자 추가(`Materials`·`PartKinds` 와 같은 꼴) |

**실측**

```
[전송 모드]
  Enter    IsActive=True · Follow null(분리) · 카메라가 발전기 자리(-1.50, 0.50)로 이동
  입력     Move 액션만 켜짐(Interact=False) · MoveValue (1,0) 정상
  이동     오른쪽 3초 -> x 9.50 (한계 9.50) · 위 3초 -> y 11.50 (한계 11.50)
           powerRange 8 + 여유 3 정사각형에서 정확히 멈춘다
  Exit     Follow=TestPlayer 복원 · 액션맵 전체 복구

[첫 시작 곡괭이]
  ToolDictionary.Tools 4개 · 곡괭이 tool_pickaxe / canMineBlocks=True
  칸 0 rod(Curated) -> stone_rod · 칸 1 pickaxe_head(Curated) -> stone_pickaxe_head
  세이브 없는 상태로 PlayerSave.Load() -> 손 칸(30) = tool_pickaxe x1 [stone,stone] 48/48
  채워진 칸 1개 (곡괭이만)
  ※ 사용자 세이브는 안 건드렸다 — 없는 경로를 가리켜 흉내 내고 인벤토리는 스냅샷 복구
컴파일 · 콘솔 에러 0 · 경고 0
```

### W-2. 짚어 둘 점

- [ ] **핫바 시작 인덱스(30)를 베끼지 않았다** — `InventoryHotBarUI.SelectedInventoryIndex` 에 묻는다.
      그 숫자는 핫바의 직렬화 필드라 베껴 두면 둘이 갈린다. 손 칸이 차 있으면 `RecipeSolver.TryAdd` 로 떨어진다.
- [ ] **"처음 시작" 의 판정은 `player.dat` 이 없는 것**이다. 월드 세이브만 두고 플레이어 세이브를 지우면
      곡괭이를 다시 받는다(의도한 동작으로 본다). 지하에서는 `Load` 가 위에서 되돌아가므로 왕복해도 안 받는다.
- [ ] 돌 곡괭이 내구도는 **48**(기준 100 × 돌 handleFactor × 돌 durabilityFactor). 벽 한 칸에 1 소모.
- [ ] `panMargin` 기본 3칸. 더 키우면 청크가 안 불린 빈 화면이 보인다 — 화면을 더 넓게 쓰려면
      `MapGenerator` 의 로드 창을 카메라 기준으로 바꾸는 별건이 필요하다.
- [ ] 전송 모드에서 화면을 옮겨도 **오버레이는 로드된 기계만 칠한다**(`MapGenerator.LoadedMachines`).
      플레이어에게서 먼 곳의 기계는 애초에 링크 대상이 아니므로 지금은 문제가 없다.

---

## X. 시연용 공장 (2026-08-20) — 맵 좌측 x -29..-11, y -7..6

암반을 272칸 파고 세 공정을 놓았다. **세이브에 남는 실제 콘텐츠다**(테스트 배치물이 아니다).
기지에서 y=0 통로(x -10..-5)로 이어진다.

| 공정 | 구성 | 좌표 |
|---|---|---|
| **A 채석·분쇄·추출** | 자원 생성기 → 전기 분쇄기 → 0-0 추출기 → 상자 | y=4, x -27 / -23 / -19 / -15 |
| **B 제련** | 공급 상자(철 원석) → 화로 → 산출 상자 | y=-1, x -27 / -23 / -19 |
| **C 유체** | 석유 웅덩이+펌프 → 증류기 → 가스 발전기 | y=-5, x -26 / -23 / -18(-4) |
| 발전 | 화력 발전기 3대(석탄) + 가스 발전기 1대(가솔린) | (-25,1) (-23,1) (-21,1) / (-18,-4) |

기계 15 · 파이프 22(아이템 15 · 액체 7). 상자 면은 렌치 규약대로 Extract/Insert 를 지정했다.

**실측 (약 40초 구동 후)**

```
A  자원생성기 가동 · 분쇄기 가동 · 추출기 가동 · 상자에 raw_iron_ore x6, raw_copper_ore x5 누적
B  화로 가동(연료 coal x46) · 산출 상자 iron_ingot x28
C  펌프 out crude_oil 8000(가득) · 증류기 out gasoline 1000+1000 · 가스 발전기 가동(in 2000)
저장 worldmap.dat 340,406바이트 갱신 · 콘솔 에러 0
```

### X-2. 이 과정에서 드러난 것

- [ ] ⚠ **액체·기체 파이프의 `throughput` 이 1 이었다** — CLAUDE.md 규약은 "유체는 1000 단위로 싣는다"
      인데 에셋이 1이라 한 통 옮기는 데 1000번이 걸렸다. **둘 다 1000 으로 고쳤다**(그제서야 C 라인이 돌았다).
- [ ] ⚠ **파이프가 못 내려놓는 짐을 붙들면 그 파이프가 통째로 막힌다.** B 라인에서 화로 연료 칸이
      64/64 로 차 있는데 파이프가 석탄 7개를 물고 와 <b>원석 공급까지 함께 멈췄다</b>. 의도된 동작이지만
      (필드에 안 쏟는다) **한 상자에 연료와 재료를 섞으면 쉽게 교착한다** — 시연에서는 상자를 원석 전용으로 비웠다.
- [ ] ⚠ **저장소에서 꺼내는 파이프는 앞 칸부터 훑는다.** 0번 칸이 안 비면 뒤 칸은 영영 안 나간다 —
      한 상자에 두 종류를 넣고 번갈아 나가길 기대하면 안 된다.
- [ ] ⚠ **전력이 구조적으로 모자란다.** 이 공장의 최대 수요는 250/s(추출기 100 · 증류기 100 · 펌프 40 · 분쇄기 10)
      인데 공급은 160/s(화력 20×3 + 가스 100)다. 화력 3대를 놓고도 부족해 라인이 간헐적으로 선다.
      뿌리는 §V 에 적은 **"17대가 `energyUseRate` 미지정으로 폴백 100/s"** 와 중간 발전기 부재다.
- [ ] ⚠ **C 라인은 자립하지 못한다.** 펌프 40/s 가 병목이라 증류기가 25초에 한 번밖에 못 돌고,
      그 산출(가솔린 500 = 1000 에너지)을 25초로 나누면 40/s 인데 펌프+증류기가 140/s 를 먹는다.
      **순 −100/s** — 지금은 가스 발전기 탱크에 가솔린 2000 을 넣어 시동해 두었다.
- [ ] **레코드만 지우면 유령이 남는다.** 파이프를 `WorldMap.RemovePlaceableAt` 으로만 지우면
      `MapGenerator.loadedPipes` 에 남아 그 칸에 아무것도 못 놓는다. 드랍도 마찬가지로 오브젝트가 남는다.
      **철거는 반드시 `MapGenerator.RemoveMachineAt`**(파이프도 이 함수가 처리한다).

## Y. 대규모 시연 공장 3개 (2026-08-20) — 발전량 상향 뒤

사용자가 `FuelTable` 을 **화력 200/s · 핵 3000/s · 가스 300(원유)/600(가솔린)/s** 로 올려서
큰 공장이 성립하게 됐다. 기존 시연장(§X, y −7..6) 위·아래로 두 구역을 더 팠다.
**전부 세이브에 남는 실제 콘텐츠다**(`worldmap.dat` 346,447 → 401,934 바이트).

### 배치

| 단지 | 자리 | 구성 |
|---|---|---|
| **D 자동 제련 단지** | x −33..−14, y 14..18 | 저장 네트워크가 공장 전체를 돌린다(아래) |
| **E 금속 가공 3줄** | x −33..−23, y 7..12 | 파이프 점대점. 강철 · 철판 · 청동판 |
| **F 화학·핵 사이클** | x −29..−9, y −18..−8 | 유체 + 우라늄 정제 + 발전 + 재처리 |

**D** — 케이블 두 줄 사이에 기계를 끼운 "서버랙" 꼴이다.
`y=18` 케이블(남면 **Extract**) / `y=17` 기계 14대 / `y=16` 케이블(북면 **Insert**, 남면 **Insert**) /
`y=15` 화력 발전기 8대 + 컨트롤러 + 드라이브 2 + 터미널.
기계 행 = 자원 생성기(강화) ×3 · 전기 분쇄기 ×3 · 0-0 추출기(전기) ×4 · 전기로 ×4.
**아이템 파이프가 한 칸도 없다** — 망이 전부 나른다. 석탄도 망이 화력 발전기 연료칸에 넣는다.

**E** — `y=7` 상자[철주괴]+상자[석탄] → **용광로(2×2)** → 상자[강철] /
`y=9` 상자[철주괴] → 압축기 → 상자[철판] / `y=11` 상자[구리]+상자[주석] → 합금 재련기 → 압축기 → 상자[청동판].

**F** — 서쪽에 화력 2대(펌프·화학기 급전), 동쪽에 핵발전소 1대(3000/s).
`물 웅덩이 + 펌프 → 화학A(산성 용액)` · `유황석 → 화학0(황 가루) → 화학A` ·
`y=−12 액체 파이프 한 줄이 산성 용액을 화학B·화학C 두 곳에 나눈다` ·
`탁한 우라늄 → 화학B → 전기로 → 압축기 → 상자[농축물] → 정밀 세공기 → 핵발전소 → 폐연료봉 → 화학C(재처리) → 상자`.

### 실측 (약 58초 구동)

```
D  네트워크 Ok · 드라이브 2/2 · 버스 36 · 셀 내용 stone x687 / copper_ingot x30 / iron_ingot x22
   생산 행 9/14 가동(전기로는 광석 4개가 모일 때까지 쉰다) · 화력 8대 석탄 자동 공급 확인
E  강철 215 · 철판 271 · 청동판 269   (씨앗 소진 후 재보급: 철주괴 4000 · 석탄 1000 · 구리 1500 · 주석 500)
F  핵발전소 가동(연료봉 8) · 재처리 상자에 uranium_concentrate + lead_ingot 축적
   연료봉 10개를 정밀 세공기가 실제로 조립했고, 폐연료봉이 화학C 로 돌아와 농축물로 재생됐다
저장 401,934바이트 · 콘솔 에러 0
```

### Y-2. 이 과정에서 드러난 것

- [ ] ⚠ **플레이 중 스크립트를 저장하면 그 세션의 월드 저장이 통째로 실패한다.** 도메인 리로드로
      `WorldMap.chunks` 가 null 이 되는데 `isLoaded` 는 true 로 남아, `Save` 가 가드를 통과한 뒤 NRE 를 낸다.
      `SafeFile` 은 "기록 실패" 로만 알려 줘서 원인을 찾기 어려웠다. **`Save` 에 명시적 가드+에러 로그를 넣었다**
      (`WorldMap.cs`). 원본은 안전하지만 **그 세션에서 지은 것은 사라진다.**
- [ ] ⚠ **`FuelTable` 이 에셋을 이긴다.** 사용자가 `ThermalGenerator.fuelBurnRate` 20→200,
      `NuclearPlant` 500→3000 으로 고쳤는데 발전량은 그대로였다(표에 행이 있는 기계라서).
      CLAUDE.md §연료 에 경고를 적었다. **에셋의 값을 아예 지우거나 표로 옮기는 정리가 필요하다.**
- [ ] ⚠ **여러 칸 기계는 발자국 전체가 파이프에 닿는다.** 2×2 용광로의 `(-28,8)` 이 한 칸 위 라인의
      파이프에 잡혀 강철이 철판 상자로 샜다 — 그 면을 `Cut` 으로 막아 고쳤다.
- [ ] ⚠ **입력이 여러 칸인 기계는 칸 선점이 필요하다**(용광로 철+석탄, 합금 재련기 구리+주석).
      먼저 온 재료가 두 칸을 다 먹으면 영구 교착이다. 지금은 1개씩 미리 넣어 종류를 고정해 두었다.
      → 구조적 해결은 "레시피가 요구하는 종류당 한 칸" 같은 규칙이 필요하다(미정).
- [ ] **강화 자원 생성기 · 화학 처리기에 스프라이트가 없다**(게임에 `No Asset` 로 뜬다).
      D 에 3대, F 에 4대라 스크린샷에서 눈에 띈다.
- [ ] **분쇄기가 D 의 병목**이다 — 자원 생성기 3대가 6개/s 를 내는데 분쇄기 3대가 3개/s 만 먹어
      셀에 `stone` 이 계속 쌓인다(58초에 687개). 분쇄기를 늘리거나 생성기를 줄이면 맞는다.
- [ ] **E 는 자립하지 않는다** — 상자에 넣어 둔 재료를 소모하는 시연 라인이다.
      망에 물리면 자동 보급이 되지만, 압축기가 `compress_brick` 때문에 **모래를 받아** 입력칸이
      막힐 수 있어 일부러 분리했다. (해결하려면 기계별 입력 필터가 필요하다.)

### Y-3. 재료 상자 → 아이템 저장소 (2026-08-20)

시연 도중 재료가 떨어져 라인이 서는 일이 없게, **공급 상자를 전부 `Machine:ItemStorage` 로 교체**하고
효율 모듈 8개를 꽂아 **한 칸 9,216개**(= `baseCapacity 1024` + `capacityPerUpgrade 1024` × 8)로 채웠다.
**산출 상자는 그대로 뒀다** — 아이템 저장소는 한 종류만 담아서, 옛 A 라인 산출(광석 7종)이나
재처리 산출(농축물 + 납)처럼 **여러 종류가 들어오는 자리에는 쓸 수 없다.**

| 자리 | 담은 것 | 쓰는 곳 |
|---|---|---|
| (−33,7) + 예비 (−31,8) | 철 주괴 ×2 | E 강철 라인(용광로) |
| (−30,8) | 석탄 | E 강철 라인 |
| (−33,9) | 철 주괴 | E 철판 라인 |
| (−33,11) / (−30,12) | 구리 주괴 / 주석 주괴 | E 청동 라인 |
| (−25,−15) / (−21,−9) | 유황석 / 탁한 우라늄 | F 화학·우라늄 |
| (−27,−1) + 예비 (−26,−2) | 철 원석 ×2 | 옛 B 제련 라인 |
| **(−28,−14)** | 석탄 | F 화력 2대 — **새 파이프 1칸이 위아래 발전기 둘에 함께 넣는다** |
| **(−26,2)** | 석탄 | 옛 화력 3대 — y=2 파이프 5칸 |
| **(−24,−2)** | 석탄 | 옛 화로 |
| **(−16,−9) / (−14,−7)** | 납 / 강화 합금 | F 정밀 세공기(연료봉 조립) |

**실측 소모율(66초)과 남은 시간**

```
옛B 철 원석  4.7/s -> 저장소 2개 18,432 = 약 65분
E 강철 철주괴 2.1/s -> 저장소 2개 18,432 = 약 145분
E 철판 철주괴 1.0/s / 구리 0.8/s / 유황석 1.0/s -> 각 9,216 = 150분 이상
석탄·납·강화합금 -> 0.2/s 이하 = 반나절
```

- [ ] ⚠ **9,216 이 한 칸의 상한이다**(칸 1 × maxStack 8 모듈). 더 길게 가려면 **저장소를 하나 더 붙이거나**
      (같은 파이프가 이웃을 N→E→S→W 순으로 훑어 번갈아 꺼낸다 — 옛B·강철 라인이 그렇게 두 개다)
      `StorageBlock.baseCapacity` 를 올려야 한다. **자동 보급의 정답은 D 처럼 저장 네트워크에 물리는 것**인데,
      압축기가 `compress_brick` 때문에 모래를 받아 입력칸이 막힐 수 있어 E 는 일부러 분리해 두었다.
- [ ] ⚠ **칸 선점은 "두 칸이 동시에 비지 않는 동안" 만 유효하다.** 합금 재련기는 지금 구리 1칸 · 주석 2칸인데,
      구리가 0 이 되는 순간 주석 짐이 도착하면 **주석이 세 칸을 다 먹어 영구 교착**이다. 지금은 공급이
      소비보다 빨라 안 걸리지만, 구조적으로는 **기계별 입력 필터**가 있어야 없어진다.
- [ ] 상자를 철거하면 **상자 아이템이 바닥에 떨어진다**(`RemoveMachineAt → DropSelf`). 교체 작업에서는
      내용물을 먼저 비우고, 떨어진 상자 8개를 `chunk.RemoveDrop` + `NotifyDropRemoved` + `Destroy` 로 치웠다.
